# crumble-phone — 작업 인수인계

쿠키런: 크럼블을 **PC 없이 폰에서** 자동으로 돌리는 안드로이드 앱.
PC용 봇([SOHADA2/crumble-bot](https://github.com/SOHADA2/crumble-bot))의 판정·좌표를 그대로 옮겨 왔다.
**컴퓨터를 옮겨 다니며 작업할 때 이 문서가 기준이다. 새 PC에서는 여기부터 읽는다.**

---

## 1. 좌표와 해상도 (`Coords.kt`)

코드의 모든 좌표는 **1440x3120 설계 좌표**로 적는다(PC 봇과 같은 기준. 대상 폰인 갤럭시 S25 울트라가
마침 이 해상도라 처음엔 환산이 없었다). 다른 기기에서는 `Coords` 가 한 번만 환산한다.

**좌표가 바깥으로 나가는 곳은 네 군데뿐이다.** 새 코드가 그 밖에서 화면 픽셀을 직접 만지면 안 된다.

| 곳 | 무엇을 |
|---|---|
| `Screen.px` | 화면 한 점 읽기 |
| `TapService.tap` / `swipe` | 탭·드래그 넣기 |
| `Ocr.readNumber` / `readText` | 숫자 읽을 영역 자르기 |
| `Shortcut.findTobol` | 아이콘 찾는 창 |

- 축마다 배율을 따로 둔다(`sx`, `sy`). 3120 vs 3200 처럼 비율이 조금 달라도 더 정확하다.
- **넓이에 비례하는 값은 `Coords.area()`** 로 환산한다(픽셀 개수 임계 등 — 배율의 제곱으로 준다).
- `findTobol` 은 찾은 자리를 **다시 설계 좌표로** 돌려준다. 탭할 때 `TapService` 가 또 환산하므로,
  화면 좌표를 그대로 넘기면 두 번 곱해진다.

### 넓은 화면에서는 두 가지 경우가 있다 — 화면을 봐야 갈린다
같은 비율(9:19.5)이면 배율만 곱하면 된다: **1440x3120 · 1080x2340 · 720x1560**.
문제는 태블릿처럼 **비율이 다른** 화면인데, 여기서 갈린다.

| | 게임이 하는 일 | 우리가 할 수 있는 것 |
|---|---|---|
| **① 레터박스** | 9:19.5 를 지키고 남는 자리를 검은 띠로 | **검은 띠 안쪽만 쓰면 그대로 돈다** |
| **② UI 재배치** | 넓은 화면을 실제로 활용(아이콘 열이 바깥으로) | 좌표를 새로 재는 수밖에 없다 |

> **화면 크기만 보고 미리 막으면 안 된다.** ①이라서 사실은 잘 도는 기기까지 못 쓰게 된다.
> 처음에 그렇게 만들었다가 고쳤다.

`Coords.detect(bitmap)` 이 **게임 화면을 실제로 보고** 가른다.
- 네 변에서 안쪽으로 훑으며 **거의 완전한 검정**(모든 채널 ≤ 12)인 줄을 띠로 본다.
  게임 화면 자체도 어두울 수 있어서 문턱을 아주 낮게 잡았다.
- 화면의 1/4 넘게는 잘라내지 않는다.
- 잘라 낸 결과의 **비율이 설계와 맞을 때만** 받아들인다. 아니면 화면 전체로 되돌리고
  `ratioOk = false` (= ②로 판단).

### 언제 판단하나 — 게임을 띄운 직후
`Runner.bringGameToFront` 가 `waitGameReady()` 다음에 한 장 찍어 `detect()` 한다.
거기서 ②로 판정되면 그 자리에서 이유를 남기고 시작하지 않는다.

- `Runner.guard()` 는 **비율을 보지 않는다.** 게임을 띄우기 전에는 알 수 없다.
- 관제 화면도 미리 막지 않는다. 판단 전에는 "시작하면 게임 화면을 보고 다시 판단합니다" 라고만 한다.
- 매 실행마다 다시 본다(`redetect()`). 기기·회전·게임 판이 바뀔 수 있다.

②로 판정된 기기를 지원하려면 **좌표를 새로 재야 한다.** 기기별 좌표표를 두거나,
지금 토벌전 바로가기(`Shortcut.findTobol`)처럼 **픽셀로 찾는 방식**을 늘리는 길이 있다.

### ⚠️ `atMain` 은 '가운데만 덮는 팝업' 을 못 잡는다
```
atMain = 독이 나무색  AND  하단 중앙 주황 ✕ 없음
```
자동사냥 보상 팝업처럼 **화면 가운데만 덮고 하단 독은 그대로 보이는 창**은 이 판정을 통과한다.
그러면 `resetToMain` 이 "이미 메인" 하고 아무것도 안 하고 빠져나가고, 그다음 누르는 탭은
**전부 그 팝업이 먹는다.** 진입이 조용히 실패하고, 재시도해도 매번 같은 자리에서 같은 일이 반복된다.

**고친 방법 — 길이 막히면 메인으로 보여도 한 번 치워 본다.**
`Runner.clearPopups()` 를 진입 실패 경로에 넣었다(토벌전·일일 던전·아레나 모두).
순서는 **안전한 것부터**다:

| 순서 | 조건 | 하는 일 |
|---|---|---|
| 1 | 확인창이 보이면 | **왼쪽만** 누른다 (오른쪽 주황은 어느 창에서든 금지) |
| 2 | 하단 주황 ✕ 가 **보이면** | 그 ✕ 를 누른다 |
| 3 | 그 밖 | 뒤로가기 ↔ 팝업 바깥(전장 빈 곳) 번갈아 |

> **✕ 는 보일 때만 누른다.** 화면을 안 보고 좌표를 누르는 짓은 이 프로젝트에서 이미 한 번 되돌렸다.

`resetToMain` 에도 같은 수단을 넣었다. 예전엔 **뒤로가기 하나뿐**이라, 뒤로가기로 안 닫히는
팝업을 만나면 8번 헛돌다 `stuck` 으로 끝났다.

### 시작 조건은 `Runner.guard()` 한 곳에
콘텐츠마다 같은 검사를 베껴 두면 새 검사를 넣을 때 한 군데를 빠뜨린다.
`running` / 접근성 / 화면 읽기 / **화면 비율** 을 여기서 한 번에 본다.

## 2. 새 PC 준비

```powershell
git clone https://github.com/SOHADA2/crumble-phone.git
cd crumble-phone
powershell -ExecutionPolicy Bypass -File setup-dev.ps1        # 기본 D:\android-dev
.\dev.ps1 install                                             # 빌드 + 폰에 설치 + 권한 복구
```

`setup-dev.ps1` 이 JDK17 · Android SDK(플랫폼 34) · Gradle 을 **한 폴더에** 받는다(시스템은 안 건드림).
Android Studio 는 필요 없다.

| 명령 | 하는 일 |
|---|---|
| `.\dev.ps1 build` | APK 만 빌드 |
| `.\dev.ps1 install` | 빌드 + 설치 + 접근성/오버레이 권한 복구 + 앱 실행 |
| `.\dev.ps1 log` | 앱 기록 30줄 |
| `.\dev.ps1 shot` | 폰 화면 캡처를 `shots\` 에 저장 |

### ⚠️ 재설치할 때마다 사람이 눌러야 하는 것 하나
**화면 읽기(MediaProjection) 동의**는 세션 단위라 재설치하면 초기화된다.
앱에서 `[화면 읽기 허용]` → **"앱 하나 공유"를 "전체 화면"으로 바꾸고** → `[다음]` → `[지금 시작]`.
("앱 하나 공유"로 두면 우리 앱 자신만 찍혀서 게임을 못 읽는다.)
접근성·오버레이 권한은 `dev.ps1` 이 adb 로 되살리므로 다시 안 물어본다.
→ **빌드 횟수를 아껴서 여러 수정을 한 번에 묶을 것.**

## 3. 배포와 앱 안 자동 업데이트

`main` 에 push 하면 **GitHub Actions 가 APK 를 대신 빌드**해 `latest` 릴리스에 붙인다
(`app-debug.apk` 와 `latest.json`). 개발 PC 가 없어도(클라우드에서 작업해도) 폰에 넣을 APK 가 나온다.
**폰에 붙어서 하는 실기 검증만** adb 가 연결된 PC 가 필요하다.

### ⏳ 앱 안 [업데이트] 는 아직 안 산다 — 마지막 한 칸이 남았다
앱 코드는 다 됐다. **저장소가 비공개라 릴리스 주소가 로그인을 요구해서** 앱이 못 받을 뿐이다.
지금은 업데이트 확인이 조용히 실패하고 넘어간다(사용자에게 아무 말도 안 한다).

앱에 토큰을 넣는 건 답이 아니다 — **APK 를 뜯으면 누구나 저장소에 접근하게 된다.** 하지 말 것.

끝내는 길은 둘이고, **어느 쪽이든 코드 한 줄 + 워크플로 한 단계**다.

| | 할 일 | 코드 |
|---|---|---|
| **A. 이 저장소를 공개로** | Settings → Danger Zone → Change visibility | **이미 이 상태에 맞춰져 있다.** 공개로 바꾸는 순간 바로 동작한다 |
| **B. 공개 릴리스 저장소 분리** | ① 공개 저장소 `crumble-phone-releases` 생성 ② 그 저장소에 Contents:RW 토큰 발급 ③ 이 저장소 시크릿 `RELEASES_TOKEN` 에 등록 | `Updater.BASE` 를 그 저장소로 바꾸고, 워크플로에 그 저장소로 올리는 `softprops/action-gh-release` 단계를 하나 더 둔다(`repository:` + `token:`) |

A 는 소스가 공개된다. B 는 소스가 비공개로 남지만 준비가 세 단계다.
**B 로 갈 예정이다(PC 에서 작업할 때 저장소를 따로 만들기로 했다).**

> GitHub 은 **토큰 발급에 API 가 없다.** 계정 주인이 브라우저에서 직접 만들어야 한다 —
> 자동화로 대신 해 줄 수 없는 유일한 단계다. 저장소 생성도 이 세션의 GitHub 연결로는 403 이 났다.

### 앱 안 [업데이트]
켤 때 조용히 한 번 확인하고, 새 판이 있으면 줄에 `새 버전 1.12 있음` 이 파랗게 뜬다.
누르면 **받아서 설치 화면까지 띄운다.** 마지막 '설치' 확인은 안드로이드가 반드시 사용자에게 묻는다
(사이드로드 앱은 예외가 없다 — 기기 관리자 앱만 건너뛸 수 있다).

`Updater.kt` 가 릴리스의 `latest.json` 을 읽어 `versionCode` 를 지금 판과 비교하고,
APK 를 받아 `PackageInstaller` 세션으로 넘긴다(FileProvider 가 필요 없다).

> ⚠️ 주소는 **태그를 직접** 가리켜야 한다(`/releases/download/latest/...`).
> `/releases/latest` 는 프리릴리스를 건너뛰어서 404 가 난다.

### ★ 서명 키를 저장소에 고정해 뒀다 — 이게 없으면 업데이트가 성립하지 않는다
`app/build.gradle.kts` 가 `debug.keystore` 를 명시적으로 쓴다.
**이 설정이 없으면 AGP 가 빌드할 때마다 `~/.android/debug.keystore` 를 새로 만드는데,
CI 러너는 매번 새 기계라 매 빌드 서명이 달라진다.** 서명이 다르면 안드로이드가 덮어쓰기 설치를
거부한다("앱이 설치되지 않았습니다"). debug 키라 비밀번호는 안드로이드 관례값(`android`) 그대로다.

> ⚠️ **키를 박기 전(v1.10 이전)에 깔았던 판이 폰에 있으면 한 번은 지우고 새로 깔아야 한다.**
> 그때까지는 빌드마다 서명이 달랐다. 한 번 지우고 깔면 그다음부터는 계속 덮어쓰기가 된다.

버전은 `versionCode = GitHub Actions 실행 번호`, `versionName = 1.<번호>` 다. 계속 커지므로
앱이 새 판인지 숫자만 비교하면 된다.

### 처음 설치할 때 (폰에 앱이 아직 없을 때)
폰 브라우저로 아래를 열어 `app-debug.apk` 를 받는다. 그다음부터는 앱 안 [업데이트] 로 끝난다.

    https://github.com/SOHADA2/crumble-phone/releases/tag/latest

## 4. 구조

| 파일 | 역할 | PC 봇에서 대응 |
|---|---|---|
| `TapService.kt` | 접근성 서비스 — 탭·드래그·뒤로가기 | `adb shell input tap/swipe/keyevent 4` |
| `CaptureService.kt` | MediaProjection 포그라운드 서비스 — 화면 읽기 | `adb exec-out screencap` |
| `Screen.kt` | 화면 판정(메인·확인창·로비·결과창)과 좌표 | `dev.ps1` + `tobol.ps1` |
| `Runner.kt` | 콘텐츠 루프(현재 토벌전) | `tobol.ps1` |
| `Ocr.kt` | 숫자 읽기(ML Kit) | `ocr.ps1` |
| `Overlay.kt` | 게임 위 상태 알약 | 관제창 |
| `MainActivity.kt` | 관제 화면 | `CrumbleBotWpf.ps1` |

### 화면 읽기를 끄는 길 (없으면 못 끈다)
MediaProjection 은 **포그라운드 서비스**라 앱을 최근목록에서 밀어내도 안 죽는다.
끄는 길을 안 만들면 '화면을 읽는 중' 알림이 계속 남는다(실제로 그랬다). 지금은 세 군데다.

| 끄는 곳 | 경로 |
|---|---|
| 알림의 `[끄기]` | `PendingIntent.getForegroundService` → `ACTION_STOP` |
| 관제 화면 `[화면 읽기 끄기]` | `CaptureService.stop(ctx)` — 켜져 있을 때만 보인다 |
| 상태바에서 공유 끊기 | `MediaProjection.Callback.onStop` |

셋 다 같은 `shutdown()` 으로 모인다 — **돌던 것 멈춤 → 오버레이 걷기 → 캡처 놓기 → 알림 제거**.

> ⚠️ `START_STICKY` 를 쓰면 안 된다. 시스템이 서비스를 되살려도 화면 읽기 동의는 **1회용 토큰**이라
> 같이 살아나지 않는다. 그래서 캡처는 안 되는데 알림만 남고, 꺼도 다시 뜨는 것처럼 보인다.
> `START_NOT_STICKY` 로 둔다.
> 같은 이유로 `onStop` 콜백에서 `instance` 를 반드시 비워야 한다. 안 그러면 앱은 '읽는 중'인 줄 알고
> 영영 빈 화면을 붙든다.

### ⚠️ 게임 패키지를 하나로 박으면 안 된다 (`GameApp.kt`)
같은 게임이라도 **스토어마다 패키지가 다르다.** 구글 플레이로 깔았다가 **갤럭시 스토어**로 다시 깔면
앱이 게임을 못 알아본다(실제로 겪었다 — `게임을 찾지 못했어요`).

- `GameApp` 이 런처에 뜨는 앱 중 **`com.devsisters.` 로 시작하는 것**을 찾아 `Prefs.gamePackage` 에 기억한다.
- 자동 탐지가 빗나가면 **도구 → 게임 앱** 에서 손으로 고른다.
- 매니페스트 `<queries>` 에 `MAIN/LAUNCHER` 인텐트를 넣어 런처 앱을 볼 수 있게 했다.
  `QUERY_ALL_PACKAGES` 는 안 쓴다 — 이걸로 충분하고 권한이 더 얌전하다.

> **화면 판정·좌표는 스토어와 무관하게 같다.** 패키지만 맞으면 나머지는 그대로 돈다.
> 접근성 서비스도 `packageNames` 로 제한하지 않았으므로 어느 판이든 탭이 들어간다.

### 안드로이드 규칙 메모
- `getMediaProjection()` **전에** `foregroundServiceType=mediaProjection` 으로 포그라운드 전환이 끝나 있어야 한다(14+).
- 매니페스트 `<queries>` 에 `com.devsisters.cc` 가 없으면 `getLaunchIntentForPackage` 가 null 을 준다(11+).
- 게임이 스스로 `KEEP_SCREEN_ON` 을 걸어서 화면은 안 꺼진다. 대신 폰이 전용으로 묶이고 발열·배터리를 감수해야 한다.

## 5. 관제 UI 방침

**이미지 파일도 커스텀 글꼴도 쓰지 않는다.** 이건 그대로다 — APK 를 가볍게 유지하고,
새 파일이 안 생기니 배포에서 신경 쓸 것도 없다. 대신 *"공식 앱처럼"* 을 이렇게 낸다.

### 구조는 iOS 의 '묶음 목록(Inset Grouped)'
```
큰 제목 → 지금 상태 카드 → (도는 중이면 멈추기) → 구역별 묶음 목록 → 기록
```
- **콘텐츠는 콘텐츠끼리 한 목록에.** '자동 실행' 에 `퀘스트` · `토벌전` · `보상만 받기` 가 나란히
  있고, 각 줄에 부제로 무엇을 하는지 적는다. 일일 던전·아레나가 붙어도 **줄 하나만 늘면 된다.**
  > 처음엔 `[봇 본체 시작]` 만 주 버튼으로 크게 뒀는데, 그러면 콘텐츠가 늘 때 구조가 깨진다.
  > '봇 본체' 라는 이름도 PC 봇 용어라 화면에서는 뜻이 안 통했다 → **`퀘스트`** 로 바꿨다
  > (코드 파일은 `Chores.kt` 그대로다).
- **주 버튼은 돌고 있을 때만 나온다** — 빨간 `[멈추기]` 하나. 쉴 때는 화면에 버튼이 없고
  목록에서 고른다. 한 번에 하나만 돌므로 도는 동안 나머지 줄은 흐려진다.
- 묶음은 `radius 12` 카드 하나에 셀을 넣고 **모서리 클리핑은 카드가 한 번만** 한다.
  셀마다 둥글리면 iOS 처럼 안 보인다.
- 셀 사이 선은 `0.5dp`, 왼쪽을 `16dp` 들여쓴다.
- '준비' 구역은 셋 다 되면 통째로 사라진다. 할 일이 없으면 화면에서도 없어야 한다.

### 위계는 글자 크기로 낸다 (색을 늘리지 말 것)
| 크기 | 무게 | 쓰는 곳 |
|---|---|---|
| 34 | Bold | 큰 제목 |
| 22 | Medium | 지금 하는 일 |
| 17 | Regular | 목록 줄 · 버튼 |
| 13 | Regular | 목록 줄의 부제 |
| 15 | Regular | 설명 |
| 13 | Medium | 구역 이름 · 상태 꼬리표 |

### 색 — 크럼블 메인 화면의 따뜻한 갈색 (2026-09-06, iOS 시스템 색에서 갈아탐)
처음엔 iOS 시스템 색(검정·흰색·시스템 블루)으로 '공식 앱' 느낌을 냈는데, 게임 도우미로는
남의 앱 같았다. **PC 관제창이 이미 겪고 확정해 둔 팔레트를 그대로 가져왔다.**

> ⚠️ 크럼블의 정체성은 **메인 화면의 갈색**이다. 길드·던전 팝업의 밝은 청록이 아니다.
> PC 쪽에서 그 청록으로 칠했다가 *"쿠키런의 다른 게임 같다"* 는 지적을 받고 되돌린 이력이 있다.

| 요소 | 게임 실측 | 앱 |
|---|---|---|
| 하단 독(제일 어두운 판) | #281E1C | 바탕 #231A18 · 더 어두운 판 #1B1412 |
| 돌 패널(플레이트 강화) | #3A2C2A | 카드 #382A27 |
| 테두리 | 탄/황토 | #8A6A52 / #6B4F42 |
| 글씨 | 크림 | #E8D9C8 / #BCA491 / #8A7565 |
| 숫자 강조 | 금색 | #FFC94A (구역 이름·진행률 막대) |
| 주 버튼 | 주황 #FD9500 | #F0921E |

**라이트/다크를 나누지 않는다** — 게임 스킨은 한 벌이어야 게임처럼 보인다.
`Theme.of()` 는 시스템 설정을 무시하고 늘 이 한 벌을 준다.

#### 두툼하게 만드는 두 가지 (`Theme.chunky` / `Theme.card`)
카툰 느낌은 색보다 **두께**에서 나온다.
- `card(radius, stroke)` — 카드에 **2dp 탄색 테두리**. 게임 패널처럼 보이게 하는 핵심이다.
- `chunky(base, radius, stroke, depth)` — 버튼. 아래 칸에 어두운 판(`shade(base,0.55)`)을 깔고
  그 위에 위가 밝은 그라데이션 면을 `depth` 만큼 띄워 올린다 → 눌리지 않은 동안 **두께가 보인다**.
  외곽선·그림자 색은 `shade()` 로 **계산**한다. 색마다 손으로 고르면 새 색을 넣을 때마다 틀어진다.

#### 한 색은 한 역할로만
바탕이면 바탕, 글씨면 글씨, 테두리면 테두리. 겹쳐 쓰면 톤을 통째로 바꿀 때 전역 치환이 안 된다.
**이 규칙 덕에 이번 전환도 `Theme.kt` 한 파일 + 호출부 몇 줄로 끝났다.**

#### 알약은 게임의 **'스테이지 바' 판**을 그대로 본떴다 (2026-09-06)
사용자가 게임 화면 조각을 보내며 *"플로팅바를 이런 느낌으로"* 라고 했다. 실측해서 옮겼다.

| 요소 | 게임 실측 | 알약 |
|---|---|---|
| 어두운 판 | (44,42,47)~(62,50,54) | `#F02E272C` |
| **외곽선** | 거의 검정 (0,0,12) | **`#0B0609` 3dp** |
| 금색 글자 | (248,240,97) | `#F8E861` |
| 금색 바 단면 | (255,217,44)→(249,193,20)→(254,173,4)→(106,68,6) | 같은 4단 그라데이션 |

> **핵심은 테두리다.** 게임 UI 는 얇은 색선이 아니라 **두꺼운 검정**으로 둘러서 만화처럼 보인다.
> 앞서 쓰던 탄색 2dp 테두리를 검정 3dp 로 바꾸니 단번에 게임 UI 결이 됐다.
> 점·[멈추기]·패널·패널 버튼까지 **전부 같은 검은 외곽선**으로 통일했다.

진행률도 게임처럼 **금색 막대**로 보여 준다 — 검은 홈(`#15100F` + 검정 테두리) 위에서
금색 막대가 `scaleX` 로 차오른다(분모가 있는 작업일 때만, 없으면 숨긴다).

> 알약은 `Theme.kt` 를 쓰지 않는다. **게임 UI 를 직접 본뜬 색**이라 관제 화면 팔레트와 따로 간다.

#### (옛 기록) 알약도 관제 화면과 같은 톤 (`Overlay.kt`)
바탕 `#F2231A18`(하단 독 갈색) · **2dp 탄색 테두리** · 크림 글자 · 초록 점에 짙은 테두리.
[멈추기] 는 위가 밝은 빨강 그라데이션 + 짙은 외곽선. 게임 화면 위에 얹혀도 이물감이 없다.

### (옛 기록) 색은 iOS 시스템 색을 그대로 가져왔다 (`Theme.kt`)
손으로 고른 색보다 서로 잘 맞고 **라이트/다크가 이미 짝지어져 있다.**
`Theme.of(ctx)` 가 시스템 설정을 보고 한 벌을 고른다 — 화면에 색을 직접 적지 말고 여기서 꺼내 쓴다.

| 역할 | 다크 | 라이트 |
|---|---|---|
| 바탕 | `#000000` | `#F2F2F7` |
| 카드 | `#1C1C1E` | `#FFFFFF` |
| 선 | `#38383A` | `#C6C6C8` |
| 글씨 / 보조 / 흐림 | `#FFFFFF` `#98989E` `#6C6C70` | `#000000` `#6C6C70` `#A0A0A5` |
| 파랑 / 빨강 / 초록 / 주황 | `#0A84FF` `#FF453A` `#30D158` `#FF9F0A` | `#007AFF` `#FF3B30` `#34C759` `#FF9500` |

### 아이콘도 벡터로 그렸다
- 런처: `mipmap-anydpi-v26/ic_launcher.xml` (적응형) + `drawable/ic_launcher_*.xml`.
  갈색 바탕 `#C2703A` 에 크림색 쿠키. **minSdk 26 이라 적응형만 있어도 모든 기기가 쓴다**
  (PNG 를 여러 해상도로 넣을 필요가 없다). `monochrome` 도 넣어 테마 아이콘까지 지원한다.
- 알림줄: `drawable/ic_stat_bot.xml`. 시스템이 흰 실루엣으로만 그리므로 단색 한 덩어리다.
  전에 쓰던 `android.R.drawable.ic_menu_view` 는 돋보기라 무슨 앱인지 알 수 없었다.

### 진행률 막대
`Runner.progress` (0~100, **-1 이면 숨김**) 하나만 보고 상태 카드가 그린다.
`Runner.set()` 으로 새 상태를 쓰면 자동으로 -1 이 된다 — 다음 단계로 넘어갔으니 앞 막대는 의미가 없다.

> **진짜 분모가 있을 때만 채운다.** 없는 걸 지어내면 막대가 거짓말을 한다.

| 작업 | 분모 |
|---|---|
| 오븐 | 넘긴 팝업 / 15회 |
| 보스전 대기 | 지난 시간 / 35초 |
| 뽑기 결과 정리 | 닫은 횟수 / 24회 |
| 보상 받기 | 걸음 / 6 (미션 3탭 + 출석 3단계) |
| 토벌전 전투 | 지난 시간 / 90초 — **'포기까지 얼마나 남았나'** 다. 전투가 언제 끝날지는 모른다 |
| 그 밖(퀘스트 대기 등) | 없음 → 막대를 숨긴다 |

너비를 직접 바꾸면 애니메이션이 안 된다. 꽉 찬 막대를 왼쪽 기준(`pivotX=0`)으로
`scaleX` 만 줄여 두면 부드럽게 움직인다.

### 첫 실행 안내 (`SetupActivity.kt`)
켤 것이 남아 있으면 앱을 켤 때 안내가 먼저 뜬다(한 번만 강요한다. 목록의
`차근차근 안내 받기` 로 언제든 다시 연다).

**할 일의 개수는 줄일 수 없다** — 접근성도 화면 읽기도 안드로이드가 사용자에게 직접 받는
허가라 앱이 대신 켤 방법이 없다. 대신 **막히는 지점**을 없앤다.
- 지금 할 것 **하나만** 펼치고, 끝난 것은 접어서 ✓ 로 바꾼다. 1초마다 다시 보므로
  설정 화면에 다녀오면 알아서 다음 칸으로 넘어간다.
- 각 단계에서 해당 설정 화면으로 바로 보낸다. 접근성은 `:settings:fragment_args_key` 로
  우리 항목을 짚어서 연다(안 되는 기기는 그냥 목록이 열린다).

여기서 **실제로 넘어지는 두 곳**을 미리 말해 준다. 둘 다 사용자가 직접 겪은 것이다.

| 넘어지는 곳 | 안내 |
|---|---|
| 접근성 스위치가 **회색** | 안드로이드 13+ 는 스토어 밖 설치 앱의 접근성을 막는다 → 앱 정보 ⋮ → **제한된 설정 허용**. 그 화면으로 보내는 버튼을 같이 둔다 |
| 화면 읽기 동의의 **'앱 하나 공유'** | 그대로 두면 우리 앱 자신만 찍혀 게임을 못 본다 → **'전체 화면'** 으로 바꾸라고 노란 칸으로 경고 |

> 노란 경고 칸은 **여기서 실제로 넘어지는 자리에만** 쓴다. 늘려 놓으면 아무도 안 읽는다.

### 폰마다 한 번은 해야 하는 것 (없앨 수 없다)
| 단계 | 왜 |
|---|---|
| 브라우저에 '출처를 알 수 없는 앱' 허용 | 스토어 밖 설치는 무조건 |
| Play 프로텍트 차단 넘기기 | 접근성+설치 권한 조합이라 자동 차단된다. Play 스토어 → 프로필 → Play 프로텍트 → ⚙ → 검사 끄기 → 설치 → 다시 켜기 |
| 제한된 설정 허용 | 안드로이드 13+ |
| 접근성 서비스 켜기 | 탭을 넣는 핵심. 시스템 설정에서만 켤 수 있다 |

화면 읽기 동의는 **1회용 토큰**이라 앱이 저장할 수 없다. 다만 `CaptureService` 가 살아 있는
동안은 유효하므로, 껐다 켜거나 재부팅했을 때만 다시 누르면 된다.
PC 가 있으면 `dev.ps1 install` 이 접근성·오버레이를 adb 로 부여해 대부분을 건너뛴다.

### 설정 (`Prefs.kt`)
SharedPreferences 하나. 콘텐츠 코드가 Context 를 들고 다니지 않게 `Prefs.init(ctx)` 한 번 하고
어디서든 값만 읽는다. **init 전에 읽어도 죽지 않고 기본값이 나온다**(nullable 로 둔 이유).

| 설정 | 기본 | 하는 일 |
|---|---|---|
| **시험 모드** | 꺼짐 | **재화·입장권을 하나도 안 쓰고 진입까지만.** 좌표를 공짜로 확인하는 길 |
| 아레나 판수 | 10 | 5 · 10 · 20 · 30 중 탭할 때마다 돈다. 재화가 먼저 떨어지면 거기서 끝 |
| 일일 던전 소탕 | 꺼짐 | 기회를 다 쓴 뒤 **SKIP 티켓까지** 쓴다(광고 제거 보유자용) |

> **시험 모드는 그냥 편의 기능이 아니다.** `maxDungeons=0` · `maxFights=0` 같은 안전한 시험 방법을
> 코드에 넣어 놓고 **누를 방법을 안 만들면 아무 소용이 없다.** 실제로 그 상태로 한 판 나갔다.
> 각 콘텐츠는 이렇게 받는다 — 일일 던전 `0`, 아레나 `0`, 토벌전 `0`, 퀘스트는 **오븐만 건너뛴다**
> (퀘스트 순환에서 재화를 쓰는 건 오븐뿐이다).

### 무엇이 도는지 보이게 — 알림줄이 주력이다
봇이 도는 동안 사용자는 **게임 화면**을 본다. 앱을 다시 열어야만 진행을 알 수 있으면 소용이 없다.
그래서 상태를 세 군데에 같이 낸다. 셋 다 `Runner.task` / `status` / `detail` / `progress` 하나만 본다.

| 자리 | 언제 보이나 | 내용 |
|---|---|---|
| **알림줄** (`CaptureService`) | **항상** — 권한이 따로 필요 없다 | `크럼블 폰봇 · 퀘스트` / `오븐에서 장비 뽑는 중 · 7/15` + 진행 막대 |
| 게임 위 알약 (`Overlay`) | 오버레이 권한이 있을 때 | `퀘스트 · 오븐에서 장비 뽑는 중 47% · 7/15` |
| 관제 화면 상태 카드 | 앱을 열었을 때 | `● 퀘스트 도는 중` + 큰 글씨 + 막대 |

> **알림줄이 주력이다.** 오버레이는 권한이 있어야 뜨지만 알림은 항상 뜬다. 예전엔 여기가
> `화면을 읽는 중` 으로 고정돼 있어서 제일 좋은 자리를 놀리고 있었다.

- 글이 **바뀔 때만** 다시 그린다(같은 글로 계속 `notify` 하면 시스템이 눌러 버린다).
- `setOnlyAlertOnce(true)` — 글이 바뀔 때마다 소리·진동이 나면 안 된다.
- 알림을 누르면 관제 화면이 열린다. `[끄기]` 는 그대로 화면 읽기를 끈다.

### 화면은 끌 수 없다 — 대신 백라이트만 내린다
| 화면이 꺼지면 | |
|---|---|
| 화면 읽기(MediaProjection) | 미러링이 멈춘다 — 검은 화면만 읽힌다 |
| 탭 넣기(접근성 제스처) | 안 들어간다 |

둘 다 안드로이드의 벽이라 우회로가 없다. 게임이 스스로 `KEEP_SCREEN_ON` 을 걸기도 한다.

**하지만 밝기는 내릴 수 있다.** 핵심은 이것 —
**화면 읽기는 백라이트가 아니라 프레임버퍼(디지털)를 읽는다.** 그래서 백라이트를 최소로 내려도
봇이 읽는 그림은 100% 그대로다. AMOLED 라 전력·발열·번인이 같이 줄어든다.

> ⚠️ **검은 판을 덮어씌우는 방식은 안 된다.** 그건 캡처에 같이 찍혀서 게임을 못 읽는다.
> 우리가 쓰는 건 오버레이 창의 `screenBrightness` 다(그리는 게 아니라 백라이트만 건드린다).

구현은 이미 있는 알약 창에 얹었다(`Overlay.applyBrightness`). 어둡게 하는 건
**봇이 도는 중 + 설정을 켬 + 최근에 탭하지 않음** 셋이 모두 참일 때뿐이고, 하나라도 아니면
`BRIGHTNESS_OVERRIDE_NONE` 으로 되돌린다.

#### 알약의 조작 규칙 — 한 동작은 한 가지 일만
```
┌──────────────────────────────────────────┬──────────┐
│ ● 퀘스트 · 오븐에서 장비 뽑는 중 47% · 7/15 │  멈추기  │
└──────────────────────────────────────────┴──────────┘
   본문: 탭 = 잠깐 깨우기 · 끌기 = 위치 옮기기      늘 보이는 버튼
```
- **`[멈추기]` 는 늘 보인다.** 예전엔 알약을 탭해야 나왔는데 **있는 줄도 몰랐다.**
  게다가 그 탭에 밝기 깨우기까지 얹혀 있어서 한 동작이 두 가지 일을 했다.
- 버튼은 자기 탭을 먼저 가져가므로(ViewGroup 이 자식에게 먼저 넘긴다) 본문 탭과 안 겹친다.
  버튼을 잡고 끌 수는 없지만, 알약의 나머지가 전부 손잡이라 문제되지 않는다.
- 쉬는 중에는 버튼을 숨긴다 — 눌러 봐야 할 일이 없는 버튼을 두지 않는다.

#### 되돌릴 길을 반드시 남길 것
어둡게 하면 **알약이 유일한 조작점**이 된다. 그래서:
- **알약을 탭하면 15초 동안 원래 밝기**로 돌아온다(그리고 `[멈추기]` 도 같이 나온다).
- 어두울 때의 밝기를 **0 으로 두지 않는다**(`DIM = 0.03`). 완전 최소로 내리면 알약이 안 보여서
  되돌릴 수가 없다. 아주 어둡되 손으로 찾을 수는 있는 값이어야 한다.
- 알약에 옅은 흰 테두리를 줘서 어두운 화면에서도 윤곽이 보이게 했다.
- 오버레이 권한이 없으면 애초에 안 된다 → 설정에서 켤 때 안내 창을 띄운다.

### 게임 위 알약이 사라지던 두 가지 (2026-09-06 수정)
증상: **어떨 때는 떠 있고 어떨 때는 없다.**

1. **`idleTicks` 를 `show()` 에서 안 되돌렸다** ← 주범.
   `show()` 는 콘텐츠를 시작하기 **전에** 불리므로 첫 tick 때 아직 `Runner.running` 이 false 다.
   지난 번에 쉬면서 7 까지 세어 둔 값이 남아 있으면 그 한 번으로 임계(6)를 넘어 **바로 hide** 된다.
   → 첫 실행만 되고 그 뒤로는 안 뜬다. `show()` 에서 `idleTicks = 0`.
2. **관제 화면에서만 띄웠다.** 봇은 서비스로 계속 도는데 앱을 최근목록에서 밀어내면
   알약만 사라진다. → `CaptureService` 의 1초 ticker 가 `Overlay.ensure()` 로 보장한다
   (서비스는 앱이 닫혀도 살아 있고, 그 핸들러는 메인 스레드라 `addView` 가 안전하다).

> `WindowManager.LayoutParams` 의 `x`/`y` 는 **픽셀**이다. 안전 띠 `Y_MIN`~`Y_MAX` 도 픽셀이라
> 초기 위치를 `dp()` 로 주면 자가 섞인다. 지금은 둘 다 픽셀로 맞췄다.

### 알약은 글자가 길면 [멈추기] 를 잘라먹는다 (2026-09-06 수정)
창이 `WRAP_CONTENT` 라 글자가 길수록 알약이 커지는데, 화면 폭에 닿으면 **오른쪽 끝의 [멈추기] 가
잘려 빨간 조각만 보였다.** 실기 사진으로 확인.

- 글자 쪽에 상한을 뒀다 — `maxWidth = 화면폭 - 190dp`(최소 130dp)
- 넘치는 글자는 **자르지 않고 끊김 없이 흐른다**. 아래 '순환' 항목 참고.
- [멈추기] 는 `minWidth 62dp` · `weight 0` 이라 **절대 줄어들지 않는다**
- `"퀘스트 · 퀘스트 기다리는 중"` 처럼 앞말이 겹치면 앞을 뺀다(알약은 자리가 귀하다)

### 흐르는 글자는 **직접** 만든다 — 안드로이드 marquee 를 쓰지 말 것 (2026-09-06)
처음엔 `ellipsize=MARQUEE` 를 썼는데 사용자가 *"순환식이 아니라 잘려 보이고 툭툭 끊긴다"* 고 했다.
맞는 지적이고, 이유가 둘이다:
1. **marquee 는 순환이 아니다.** 끝까지 갔다가 **되돌아 튄다**(왼쪽으로 계속 흐르지 않는다).
2. 알약은 **1초마다 글자를 갈아 끼운다.** 글자가 바뀌면 marquee 가 처음부터 다시 시작한다 —
   그래서 몇 글자 흐르다 툭 끊기기를 반복했다. `초 남음` 처럼 매초 바뀌는 문구에서는 아예 안 흐른다.

**대신 이렇게 한다** (`Overlay.flow()`):
- 같은 글을 **두 벌 이어 붙이고**(`"글" + GAP + "글" + GAP`) **한 벌 너비만큼** 왼쪽으로 민다.
  한 벌을 다 밀면 화면 그림이 처음과 똑같아지므로 **이음매가 안 보인다.**
- `ValueAnimator` · `LinearInterpolator` · `INFINITE` · `RESTART`, **초당 60dp**
  (빠르면 못 읽고 느리면 안 움직이는 것 같다).
- 글자가 바뀌면 애니메이터를 다시 만들되 **`currentPlayTime` 으로 진행률을 물려받는다** → 안 튄다.
- 자르는 창은 `FrameLayout`(`clipChildren`). 그 너비에 상한을 둬서 **[멈추기] 자리를 먼저 확보**한다.
  글자가 짧으면 창을 글자 길이에 맞춰 줄여 알약이 헐렁해지지 않게 한다.
- `GAP` 은 두 벌 사이 공백이다. 없으면 끝 글자와 첫 글자가 붙어 한 단어처럼 읽힌다.

### 알약이 조작 패널이 됐다 (2026-09-06)
▾ 를 누르면 알약 아래로 펼쳐져 **콘텐츠를 바로 시작**할 수 있다
(퀘스트·보스전·토벌전·일일 던전·아레나·일일 보상 + [알약 숨기기]).
게임을 보는 중에 관제 화면으로 나갔다 올 필요가 없다.

- **▾ 는 스스로 `isClickable` 이어야 한다** — 아니면 알약의 드래그 리스너가 탭을 먼저 먹는다.
  드래그 리스너도 뿌리가 아니라 **알약 줄에만** 건다(패널 버튼이 눌려야 하므로).
- **도는 동안에는 패널을 강제로 접는다.** 알약은 화면 캡처에 같이 찍히고 그 자리 탭도 가로챈다.
  알약 자체는 안전한 세로 띠(y 120~900)에 갇혀 있지만, **패널까지 펼치면 그 아래 판정 구역
  (아이콘열 y1100~)을 침범한다.** 시작하는 순간 접고, 도는 동안 열리지 않게 잠근다.
- 콘텐츠 버튼은 도는 동안 흐리게(alpha 0.45) + 비활성.

#### 쉬는 중에 저절로 사라지던 것 (같이 고침)
`tick()` 이 `Runner.running` 이 아니면 6틱 뒤 `hide()` 했다. 알약이 **쉬는 중에 쓰라고 있는
조작 패널**이 된 지금은 정반대다 — 정작 필요할 때만 사라졌다. 자동 숨김을 지웠다.
`ensure()` 도 `running` 일 때만 띄우던 것을 **화면 읽기가 켜져 있으면 늘** 띄우도록 바꿨다.

지우고 싶으면 패널의 **[알약 숨기기]**. 이때 `dismissed` 플래그가 서서, 1초 뒤 `ensure` 가
도로 띄우지 않는다. 관제 화면에서 `show()` 를 부르면 플래그가 풀린다(명시적 요청이므로).

### 게임 위 알약(`Overlay.kt`)만 예외
게임 위에 뜨는 것이라 **관제 화면의 라이트/다크와 무관하게 언제나 어두운 반투명**으로 둔다.
게임이 밝든 어둡든 읽혀야 하기 때문이다. 모서리는 `radius 100` 으로 완전한 알약.

## 6. 실측값 — 다시 재지 말 것

| 대상 | 값 |
|---|---|
| 메인 판정 | 독(300~1150, 2820~2980) 나무색 비율 ≥0.1 **AND** 하단 중앙 주황 닫기버튼 없음(7점 중 4개 미만) |
| 확인창 | 왼쪽(330~540, 2780~2845) 청록 + 오른쪽(890~1100) 주황이 **동시에** → 왼쪽(441,2797)이 언제나 안전한 쪽 |
| 토벌 로비 | (700,2700) 주황 |
| 결과창 | (100,1600)·(1340,1600) 어두운 갈색 **AND** 하단 주황 ✕ |
| 최종 점수 | 숫자는 x400~1070 · y1400~1506 → OCR 영역 **(330,1380,830,150)**, 6자리 이상만 인정 |
| 도전하기 / 결과창 ✕ | (700,2715) / (715,2990) |
| 절전 해제 | (721,1864) → (725,2520) 드래그 |
| 오버레이 안전 띠 | **y120~900** — 판정 지점도 탭 자리도 없는 구간 |

## 6.5 토벌전 진입 — 길드 탭이 먼저다 (2026-09-05)

우측 아이콘 열은 이벤트에 따라 밀려서 실제로 사고가 났다(7-1 참고). **하단 네비는 화면이 바뀌어도
자리가 그대로라** 이쪽이 훨씬 안전하다. 그래서 길을 둘로 두고 길드 탭을 먼저 쓴다.

```
1) 하단 '길드' 탭 (905,3000) → 길드 화면 → '길드 토벌전' 타일 (280,1880)   ← 주 경로
2) 메인 우측 아이콘 열의 피냐타 바로가기 (Shortcut.findTobol)              ← 폴백
```
2)는 이미 실기에서 되는 걸 확인한 길이라 남겨 뒀다. 1)이 막히면 받아 준다.

> **길드 화면인지 확인한 뒤에만 타일을 누른다.** 아니면 아무것도 누르지 않고 물러난다.
> 화면을 안 보고 좌표를 누르는 짓은 하지 않는다 — 그 자리는 화면마다 다른 것이 있다.

### 실측 (사용자 스크린샷 3장, 이미 1440x3120 이라 그대로 읽었다)
| 판정 | 메인 | 길드 | 토벌 로비 |
|---|---|---|---|
| 청록 바탕 4점 `(60,1500) (1380,1500) (60,2700) (1390,2750)` | 0/4 | **4/4** | 4/4 |
| 타일 자리 `(200,1800)-(400,1980)` 평균 밝기 | 42.6 | **162.5** | 94.9 |
| 도전하기 `(700,2700)` | (255,255,66) | (8,38,48) | **(253,149,0)** |

- 청록 판정: `B > R*2 && B > G && B > 40` 이 3점 이상.
- `atGuild` = 청록 바탕 **그리고** 타일 밝기 ≥ 130. 청록만 보면 던전·로비도 걸린다.
- 하단 주황 ✕ 7점은 길드 7/7 · 로비 5/7 · 메인 0/7 로, 기존 서브 화면 판정과도 들어맞았다.

### 같이 고친 것 — 로비 판정이 아슬아슬했다
`atTobolLobby` 는 `(700,2700)` 이 주황이면 로비로 봤는데, **메인 화면의 같은 자리는 골드 숫자라
노랑 `(255,255,66)`** 이다. `B < 60` 을 **6 차이로** 겨우 비켜가고 있었다 — 화면이 조금만 밝아졌으면
메인을 로비로 오판했다. `G` 를 `111..199` 로 가둬 벌려 놨다(주황 G≈149 / 노랑 G=255).

## 7. 함정 모음 (전부 실제로 당한 것)

1. **우측 아이콘 열은 고정 좌표로 누르면 안 된다.** 이벤트에 따라 한 칸(150px)씩 밀린다.
   PC 기준 토벌전 바로가기 `(1340,1500)` 이 폰에서는 **'크럼블 패스'(구매 화면)** 였다.
   → 지금은 길드 탭이 주 경로다(6.5). `Shortcut.findTobol` 은 폴백으로 남아 있다.
2. **로딩 중에 판정하면 안 된다.** 고정 대기로 자르면 로딩 화면에서 뒤로가기를 눌러 '전투 중'으로 잘못 끝난다.
   → `waitGameReady()` 가 아는 화면이 보일 때까지 최대 45초 기다린다.
3. **결과창을 '어둡기'만으로 판정하면 로딩 화면을 결과창으로 오인한다.** → 주황 ✕ 를 함께 본다.
4. **점수 OCR 영역은 실측해야 한다.** PC 영역을 그대로 쓰면 숫자 아래가 잘려 `204,721` 처럼 오독한다.
5. **탭 점검·화면 보내기는 게임을 먼저 띄우고**. 앱이 앞에 있으면 앱 자신을 누르고, 캡처도 앱 화면이 찍힌다.
   (둘 다 이 함정을 실제로 밟았다 — 버튼 안에서 `GameApp.launchIntent` 로 띄우고 5초 기다린 뒤에 한다.)
6. `local.properties` 의 `sdk.dir` 는 역슬래시가 이스케이프된다 → 슬래시로 쓸 것.
7. PowerShell 에서 `adb exec-out screencap -p > file` 은 PNG 를 깨뜨린다 → `shell screencap` + `pull`.
8. PowerShell 5.1 은 네이티브 exe 의 stderr 를 `2>&1` 로 받으면 죽는다(`ErrorActionPreference='Stop'` 과 함께면 특히).

## 8. 지금까지 된 것 / 다음

**된 것**
- **토벌전 자동** — 진입·반복 도전·점수 읽기(ML Kit)·결과창 정리
- **봇 본체**(`Chores.kt`) — 보상 자동 받기(미션·출석) · 완료 퀘스트 수령 ·
  쿠키 뽑기 10회 · 가방 상자 사용 · 오븐 장비 뽑기(`Oven.kt`) · **보스 소환**
  → PC 봇 `bot.ps1` 의 퀘스트 순환이 폰에서 통째로 돈다
- 게임 위 상태 알약 · 관제 화면 · `[새 APK 받기]` 버튼(릴리스 페이지 열기)

### 봇 본체가 도는 방식 (`Chores.loop`)
매 바퀴 화면을 한 장 읽고 위에서부터 걸러 낸다. 순서가 곧 안전장치다.

| 순서 | 판정 | 하는 일 |
|---|---|---|
| 1 | 독 `cv < 0.4` (화면이 평평) | 왕관 씌우기 2번 → 안 풀리면 뒤로가기·하단X·바깥 순으로 팝업 닫기 |
| 2 | 메인이 아님 | 뒤로가기를 주력으로 단계적 닫기(공지 X 자리는 제각각이라 좌표 고정 금지) |
| 3 | 퀘스트 띠 `< -0.6` (덮임) | 바깥 → 하단X → 결과창X → 뒤로가기 순으로 치우기 |
| 4 | 퀘스트 띠 `≥ doneRatio` (완료) | 띠를 눌러 보상 수령 |
| 5 | 그 외 | 띠를 눌러 유형 확인 → 뽑기 화면이면 10회 / 가방이면 상자 / 그대로면 오븐 |
| 6 | 5도 실패 | 스테이지 클리어형 — **아무것도 안 하고 [보스전] 을 안내**한다 |

#### ★ 첫 진입을 늘 막던 것 — '자동 사냥 보상' 팝업 (2026-09-06 실기 확인)
게임에 오랜만에 들어가면 거의 **항상** 뜬다. 그런데 `waitGameReady` 가 아는 화면 넷
(메인·토벌로비·BATTLE OVER·확인창)만 보고 있어서 **45초를 기다리다 포기**했다 —
알약에 `게임 여는 중 · 로딩을 기다…` 만 뜬 채 아무것도 시작되지 않은 게 이것이다.

**고침 둘:**
1. `Screen.gameIsUp()` — 아는 화면 넷 **또는 하단 주황 ✕ 가 보이면** 게임이 뜬 것으로 본다.
   ✕ 는 게임이 다 뜬 서브 화면에만 있고 로딩 화면에는 없다(이 팝업에서 **7/7** 실측).
   무엇이 떠 있든 정리는 뒤의 `clearPopups`·`resetToMain` 이 한다.
2. `Screen.isIdleReward()` — 이 팝업은 **닫지 말고 받는다**(무료고, 안 받으면 다음에 또 막는다).
   `resetToMain`·`clearPopups`·퀘스트 팝업 사다리 세 곳 모두 ✕ 판정보다 **먼저** 본다
   (이 팝업에도 하단 ✕ 가 있어서 그냥 닫혀 버린다).

판정은 **넓은 주황 [보상 받기] 띠**로 한다 — y=2788 의 x=430·570·715·860·1000 다섯 점이
실측 전부 `(255,173,1)`. 폭이 요점이다: 확인창의 주황 버튼은 오른쪽 절반에만 있어 왼쪽 두 점이
안 걸린다. 탭 좌표 `IDLE_CLAIM (715,2788)`.

> ⚠️ 왼쪽 아래 **'행운의 보상'(주사위 x1/x2/x5)은 건드리지 않는다** — 광고를 보는 쪽일 수 있다.

#### ⚠️ '한번에 클리어 하기' 띠는 색이 정반대다 (2026-09-06 실측)
밀린 퀘스트를 한 번에 받는 버튼이 생겼다(`퀘스트 17654 까지 한번에 클리어 하기` + 열쇠 그림).
띠가 **파랑·보라**로 바뀌는데 기존 판정은 전부 '금색이냐'를 보고 있어서 **어느 가지에도 안 걸렸다** —
완료도 미완료도 덮임도 아니라 계속 탐색만 돌았다.

| 상태 | BAR 평균 RGB | `questBarRatio` |
|---|---|---|
| 완료(금색) | R ≫ B | +1.11~1.28 |
| 미완료 | | +0.16~0.58 |
| **한번에 클리어(파랑)** | **(140,105,174)** | **-0.25** |
| 팝업이 덮음 | | < -0.6 |

`Screen.isBulkClear` 는 비율이 아니라 **파랑 우세**로 본다(B > R+15 이고 B > G+25, 평균밝기 90~210).
비율 하나로 -0.25 만 집으려 하면 창이 너무 좁아 위태롭다.
띠를 눌러 받고, **세 번 눌러도 안 바뀌면 그만 두고 지나간다**(못 누르는 상태일 수 있다).

#### ★ 콘텐츠 경계 (2026-09-06 정리) — 퀘스트는 남의 일을 하지 않는다
전에는 [퀘스트] 하나가 **일일 보상 수령**(20분마다 미션·출석)과 **보스 소환**까지 같이 했다.
사용자가 퀘스트만 눌렀는데 출석 창이 열리고 보스전이 시작돼 **무엇이 도는지 알 수 없었다.**
지금은 셋을 갈랐다:

| 버튼 | 하는 일 | 안 하는 일 |
|---|---|---|
| **퀘스트** | 퀘스트 띠 보상 · 뽑기 · 상자 · 오븐 | 미션·출석 수령 ✗ · 보스 소환 ✗ |
| **보스전** | 막힌 보스를 조합 1~5 로 도전 (`Boss.kt`, PC `boss.ps1` 이식) | 그 밖의 것 ✗ |
| **일일 보상** | 미션 '모두 받기' · 출석 '받기' 한 번 | 반복 ✗ |

> 퀘스트가 보스형 퀘스트를 만나면 로그와 상태에 **"[보스전] 을 눌러 주세요"** 만 남기고
> 15분 쉬었다 다시 본다(그 사이 방치 전투로 다른 퀘스트가 찬다).

> **1이 다른 것보다 먼저인 이유**: 절전 화면에서 좌표를 누르면 아무 일도 안 일어나는데 봇은 했다고 여긴다.
> **3이 4보다 먼저인 이유**: 팝업이 띠를 덮으면 띠 비율이 음수로 튀는데, 이걸 안 거르면
> 5의 임계에 안 걸려 '미완료'로 보고 계속 헛탐색한다.

#### ⚠️ 재화를 쓰는 동작은 '탭이 먹힌 증거'가 있을 때만 (2026-09-06)
오븐 가지의 판정은 "눌렀는데 화면이 안 바뀜"이다. 그런데 이 조건은 **탭이 아예 안 들어가는
기기에서도 똑같이 참**이다. 갤럭시 탭에서 퀘스트가 하나도 안 도는 채 오븐만 돌던 게 이것이다.

그래서 `tapsProven` 을 둔다 — 이 실행에서 **미션 창이 열렸다 / 보상을 받았다 / 뽑기·가방이 열렸다**
중 하나라도 봤을 때만 참이 된다. 증거가 없으면 오븐(재화를 쓰는 유일한 동작)은 미룬다.
정상 기기는 시작 20초 안의 보상 받기에서 바로 참이 되므로 아무 영향이 없다.

### ★ 갤럭시 탭 S7+ 결론 — 레터박스가 아니다. 배율로는 절대 못 맞춘다 (2026-09-06 실측)
게임 화면을 실제로 받아서 쟀다(`1752x2800`, 비율 1.598).

| 확인한 것 | 값 |
|---|---|
| 좌우 검은 띠 | **없다** — 맨 왼쪽 열 `(73,75,84)`, 맨 오른쪽 열 `(96,68,79)` 둘 다 게임 그림 |
| 세로 | **비례한다** — 하단 네비 아이콘 줄 실측 y≈2660, 예측 `2972 × 2800/3120 = 2666` ✅ |
| 가로 | **비례하지 않는다** — 하단 네비 아이콘 x ≈ 201·418·633·…·1547 (간격 ~220, 총 폭 1346, 중심 876). 폰 좌표에 어떤 배율·앵커 규칙을 대도 이 배치가 안 나온다 |

즉 게임이 **넓은 화면용으로 UI 를 다시 배치**한다. 세로만 맞고 가로가 흩어지므로
**단일 배율로는 영원히 안 맞는다.** 앞선 몇 판(v1.31~1.36)에서 고친 것들은 전부 이 위에서
벌어진 2차 증상이었다.

#### 왜 여기까지 못 알아챘나 — `Coords.detect` 가 레터박스를 **지어냈다**
후보에 "높이에 맞춘 가운데 정렬"을 넣어 두어서, 띠가 없는데도 `1292x2800 @(230,0)` 이라는
매핑이 만들어졌다. 검증으로 쓰던 `looksLikeGame` 은 `atMain` 등의 OR 인데 `atMain` 은
"하단이 갈색이고 주황 ✕가 없다"가 전부라 **틀린 매핑도 통과시킨다.**

**고침:** 지어낸 후보(③④)를 지웠다. 이제 후보는 실제로 찾아낸 둘뿐이다 —
화면 전체(비율이 맞을 때) / `trimUniform` 이 실제로 걷어낸 띠. 못 찾으면 지어내지 않고
**비율 불일치로 두어 시작을 막는다.** 엉뚱한 자리를 누르면 재화가 나간다.

#### 남은 두 갈래
1. 게임을 **폰 비율로 고정**한다(삼성: 설정 → 디스플레이 → 전체 화면 앱 / 게임 부스터의 화면 비율).
   되면 지금 좌표가 **그대로** 먹는다. 먼저 시험할 것.
2. 안 되면 **이 기기 전용 좌표표**를 새로 잰다. 화면마다 한 장씩 필요하다
   (메인·미션·출석·뽑기·가방·오븐·길드·토벌 로비·일일 던전·아레나).

### 점검 도구는 '점검' 섹션에 모아 둔다 (도구와 섞지 말 것)
태블릿을 붙이며 진단 버튼이 넷까지 늘었다. 평소엔 쓸 일이 없는 것들이라 **도구에서 떼어내
`점검` 섹션으로 모았다.** 넷 다 게임을 진행시키지 않는다(재화를 안 쓴다).

| 버튼 | 하는 일 |
|---|---|
| 화면 점검 | 게임을 띄워 좌표·판정값·기준점을 한 번에 기록 (`Runner.checkCoords`) |
| 탭 점검 | 미션 아이콘에 결과를 받는 탭을 넣어 보고 화면이 바뀌는지 확인 |
| 진단 보내기 | 기기 정보 + 최근 기록을 **글**로 (복사 + 공유창) |
| 화면 보내기 | 게임을 띄워 5초 뒤 캡처를 **그림**으로 (공유창) |

> 예전의 `[화면 좌표 확인]` + `[지금 화면 재기]` 는 하는 일이 겹쳐 **`[화면 점검]` 하나로 합쳤다.**
> 좌표가 안 맞으면 그 자리에서 `Coords.mismatchReason()` 까지 기록에 남긴다.
>
> 콘텐츠 버튼이 좌표 불일치로 막힐 때도 **카드에는 한 줄만** 띄우고 자세한 사정은 기록에 남긴다
> — 여러 줄을 카드에 밀어 넣으면 잘려서 정작 '무엇을 하면 되는지'가 안 보인다.

### 좌표가 어긋났을 때는 결국 한 장을 봐야 한다 — 점검 → [화면 보내기]
값(비율·색)만으로는 **어디로 얼마나** 어긋났는지 못 잰다. 그래서 `Runner.shot()` 한 장을 그대로
PNG 로 만들어 공유창(`FileProvider` + `ACTION_SEND`)에 넘긴다.

사용자가 직접 스크린샷을 찍는 것보다 나은 이유: **봇 화면이 아니라 게임 화면이 확실히 찍히고**,
캡처 그대로라 잘리거나 배율이 달라질 일이 없다. 받은 PNG 에서 실제 버튼 위치를 재면
설계 좌표(1440x3120)로 되돌려 매핑이 맞는지 바로 확인된다.

> **⚠️ 캡처는 실시간이다.** 봇 앱을 보고 있는 채로 찍으면 **봇 앱이 찍힌다**(처음에 이렇게 만들었다).
> 그래서 버튼을 누르면 **게임을 먼저 띄우고 5초 뒤에** 찍는다. 그다음 공유창을 여는데, 배경에서
> 창을 못 띄우는 기기가 있을 수 있어 파일을 3분간 남겨 둔다 — 봇으로 돌아와 한 번 더 누르면
> 다시 찍지 않고 그걸 그대로 보낸다.
>
> `${applicationId}.files` FileProvider + `res/xml/file_paths.xml`(cache-path `shots/`).
> 파일을 그냥 건네면 안드로이드 7+ 가 `FileUriExposedException` 을 던진다.

### 탭이 먹는지 가리는 법 — 점검 → [탭 점검]
화면이 안 바뀔 때 원인이 셋인데(① 탭이 안 들어감 ② 엉뚱한 자리 ③ 판정이 틀림) 로그만으로는 못 가른다.
`[탭 점검]` 은 게임 메인에서 ⑴ 기준점 색을 찍고 ⑵ **결과를 받는 탭**(`TapService.tapChecked` —
`GestureResultCallback` 으로 완료/취소/거부를 가린다)을 미션 아이콘에 넣고 ⑶ 바뀌었는지 다시 본다.
60ms 로 안 먹으면 200ms 로 한 번 더 — 짧은 탭을 흘리는 기기가 있다.

`Screen.landmarks()` 는 기준점 14곳(네 귀퉁이 포함)의 RGB 를 한 줄로 낸다.
**폰에서 같은 걸 찍어 나란히 놓으면 좌표 매핑이 맞는지 바로 보인다** — 귀퉁이가 `0,0,0` 이면
게임 밖 검은 띠를 읽고 있는 것이다.

#### ⚠️ 비트맵을 함수 밖으로 넘기지 말 것 (v1.33 에서 앱이 멈춤)
`Runner.shot()` 은 새 장을 받을 때 **앞 장을 recycle 한다**(한 장 18MB). 그런데 '탭 전 화면'을
`probe(pre, …)` 로 넘겼고, probe 안에서 shot() 을 부른 뒤 `pre` 를 읽었다 →
`Can't call getPixel() on a recycled bitmap` 으로 봇이 멈췄다(화면에 '오류'로 뜬다).

**필요한 값만 미리 재서 숫자로 넘긴다** — `probe(Screen.gachaHits(b), ratio)`.
비교 판정을 새로 넣을 때마다 걸리기 쉬운 함정이다.

#### ⚠️ 화면 판정은 '절대값'이 아니라 '탭 전후 비교'로 (2026-09-06, 갤럭시 탭에서 확인)
탐색(5)이 뽑기 화면인지 보는 `gachaHits==3` 을 **탭 뒤 화면만 보고** 판단했다.
탭 S7+ 에서는 **메인 화면이 그 3점을 그대로 통과**해서, 탐색이 매번 "뽑기 화면 -> 10회 수행"으로
새고 곧바로 "10회를 눌렀는데 화면이 그대로"로 끝났다 — 퀘스트가 한 개도 안 돌았다.

고친 방법: **탭 전 화면에서도 3/3 이면 오탐**으로 보고 그 가지를 타지 않는다.
같은 원칙이 다른 판정에도 적용된다 — **어떤 화면인지 절대 색으로 못 박지 말고,
'내가 누른 뒤에 그렇게 바뀌었나'로 볼 것.** 기기마다 색·배율이 조금씩 다르다.

#### ⚠️ 4와 5는 **같은 자리를 누른다** — 태블릿에서 오븐이 제멋대로 돌던 원인 (2026-09-06 수정)
4(보상 수령)와 5(유형 탐색)의 탭 좌표가 둘 다 `QUEST_BAR` 다. 그래서 **완료인데 띠 비율이
기준보다 낮게 읽히는 기기**에서는 이렇게 흘러갔다:

```
완료 퀘스트 (탭 S7+ 에서는 0.85 미만으로 읽힘)
  → 4를 못 걸고 5로 감 → 띠를 누름 → 실제로는 보상이 수령됨
  → 화면은 메인 그대로 → 옛 코드: "화면이 안 바뀜 = 오븐 퀘스트" → 재화를 써서 오븐을 돌림
```
사용자가 본 **"보상 받아야 할 때 갑자기 다른 퀘스트를 혼자 진행"** 이 정확히 이것이다.

**고친 방법 — 임계값을 손으로 맞추지 않는다.** 탐색 탭 뒤에 띠 비율을 **다시 재서**,
`0.35` 이상 달라졌으면 띠가 새 퀘스트로 갈린 것 = 보상을 받은 것으로 본다(`PROBE_CLAIMED`).
그때 본 값에 맞춰 `doneRatio` 를 `직전값 - 0.05` 로 낮춰 학습한다(바닥 `0.45`).
다음 퀘스트부터는 4에서 바로 잡히고, 오븐은 안 돌아간다.

- 이 방식이 안전한 이유: **탐색 탭은 어차피 보상 수령과 같은 동작**이라 잘못 눌러도 잃는 게 없다.
  진짜 오븐 퀘스트면 그 탭이 아무 일도 안 해서 띠 비율이 그대로 → 오븐으로 넘어간다.
- 학습값은 앱이 도는 동안만 유지한다(저장하지 않는다). 잘못 학습된 값이 영구히 남는 편이 더 위험하다.
- 지금 기준은 **점검 → [화면 점검]** 결과 끝의 `완료기준=` 에 나온다.

### 관제 화면은 '무엇을 돌릴까'만 · 나머지는 전부 ⚙ 뒤로 (`SettingsActivity`) — 2026-09-06
| 화면 | 있는 것 |
|---|---|
| **관제** | 제목 + ⚙ · 지금 상태 카드 · **자동 실행**(콘텐츠 6개) · 준비(필요할 때만) · 기록 |
| **⚙ 설정** | 게임 · 자동 실행 설정 · 화면 · 점검 · 앱 |

묶음 순서는 **얼마나 자주 쓰는가** 순이다. 도구·점검까지 관제 화면에 있을 때는 스크롤이 길어
정작 자주 누르는 콘텐츠가 눈에 안 들어왔다.

- **게임**: 게임 앱 고르기 · 게임 켜기
- **자동 실행**: 시험 모드 · 아레나 판수 · 오븐 1회 개수
- **화면**: 화면 어둡게 · 화면 읽기 끄기
- **점검**: 화면 점검 · 탭 점검 · 진단 보내기 · 화면 보내기
- **앱**: 업데이트 · 처음 안내 다시 보기

값이 바뀌는 줄(게임 앱·업데이트·화면 읽기)만 설정 화면에서도 1초 tick 으로 갈아 끼운다.

#### ⚙ 와 ‹ 는 **같은 모양의 동그란 두툼 버튼**
글리프 하나에 `chunky()` 를 씌워 42x42 정사각으로 둔다(이미지 자산 없음).
처음엔 패딩만 준 납작한 글리프였는데 화면 구석에 색만 다른 덩어리가 떠 있는 꼴이었다.
설정 화면 왼쪽 위의 **돌아가기 ‹ 도 같은 모양**이라 짝이 맞는다 — 뒤로가기가 눈에 보여야 헤매지 않는다.

#### 알약은 앱이 앞에 있으면 접는다
관제 화면 제목 위에 알약이 겹쳐 앉는 게 보기 싫었다. `Overlay.onAppForeground(true/false)` 를
두 화면의 `onResume`/`onPause` 에서 부른다. 알약은 **게임을 보는 중에** 쓰라고 있는 것이다.

### (옛 기록) 설정은 톱니바퀴 뒤로 — 2026-09-06
관제 화면 목록 한가운데에 설정이 늘어져 있어서, **자주 누르는 것(콘텐츠)과 어쩌다 한 번 바꾸는 것**이
섞여 스크롤이 길었다. iOS 앱처럼 오른쪽 위 `⚙` 뒤로 옮겼다(안전 / 콘텐츠 / 화면 세 묶음).

목록 조각(`row`·`group`·`switchRow`…)은 `MainActivity` 안에만 있던 것을 **`Ui.kt` 의
`ListActivity` 로 올려** 두 화면이 같이 쓴다. 복사하면 한쪽만 고쳐 놓고 잊는다.
`⚙` 도 이미지가 아니라 글리프다 — 이미지 자산을 안 쓴다는 원칙 그대로.

### 오븐 (`Oven.kt`) — Auto 를 끄는 게 제일 중요하다
Auto → '자동 열기' → [시작] → 알아서 뽑힌다. 장착/판매 팝업은 **버튼 말고 바깥**을 눌러 넘긴다
(Auto 가동 중에는 이렇게 넘겨도 퀘스트 수치가 오르고 장비는 오븐에 쌓인다).

> ★ **Auto 는 어떤 경로로 끝나든 반드시 꺼야 한다.** 켜 둔 채 나가면 재화를 계속 소모한다.
> 그래서 시작에 성공한 뒤로는 전부 `try/finally` 안에서 돈다 — 사용자가 [멈추기] 를 눌러도
> 끄는 것까지는 하고 나간다.

- 한 번에 몇 개 나오는지는 **오븐 레벨마다 다르다.** 개수에 의존하지 않고 '넘긴 팝업 수'와
  '퀘스트 완료'로만 판단하고, 상한 두 개(시간·15회)로 폭주를 막는다.
  (예전에 시간 상한만 두고 완료 감지가 빗나가 140초에 팝업 55개를 뽑아 재화를 크게 낭비했다.)
- 가동 중 완료 판정 문턱은 **1.0** 이다(평소 0.85 가 아니라). '자동 열기' 패널이 뜨는 순간
  띠 영역이 0.85~0.88 로 읽혀 **0회 뽑고 헛완료**로 오판하던 사고가 있었다. 진짜 금색은 1.1+ 다.
- 스테이지 클리어형 퀘스트도 화면이 안 바뀌어 오븐과 구분이 안 된다 → **퀘스트당 한 번만** 돌려 본다.

#### ⚠️ PC 봇과 일부러 다르게 한 것: 꽉 찬 오븐의 '자동 정리' 확인창
`oven.ps1` 은 이때 **오른쪽 주황 [정리하기]** 를 눌렀다. 폰에서는 **왼쪽(안전한 쪽)으로 닫고 물러난다.**
- 확인창은 전투 종료·게임 종료·다이아 새로고침이 **전부 같은 배치**라 색만으로 못 가른다.
  잘못 누르면 전투 강제 종료 / 게임 종료 / 크리스탈 300 소모다.
- PC 쪽 [정리하기] 좌표부터가 스크린샷 기준 **추정치**라 실측된 적이 없다.
- 오븐 퀘스트 하나를 건너뛰는 손해가 훨씬 싸다. 오븐이 꽉 차면 **한 번 손으로 정리**해 주면 된다.

#### ★ 장비가 꽉 차면 뜨는 '정리하기' 창 — 여기서만 오른쪽을 누른다 (2026-09-06 실측)
장비 30개쯤 뽑으면 **'자동 열기 결과'** 창이 뜬다
(*"전투력이 오르는 장비만 자동으로 착용하고 나머지 장비는 모두 판매합니다"*)
→ 왼쪽 **[그만두기]**(청록) / 오른쪽 **[정리하기]**(주황).
**안 풀면 그때부터 아무것도 안 뽑히고 남은 시간 내내 헛돈다.**

⚠️ 이 창은 위험한 확인창(전투 종료·게임 종료·크리스탈 새로고침)과 **버튼 자리가 똑같다** —
실측으로 확인했다: `(430,2790)=(19,173,183)` 청록 / `(990,2790)=(251,149,4)` 주황.
그래서 `isConfirmDialog` 가 그대로 걸리고, 안전 규칙대로 왼쪽을 눌러 오븐이 막혀 있었다.

**가르는 신호는 창의 넓이다.** 위험한 확인창은 가운데 작은 모달이라 화면 폭을 못 채운다.
이 창은 오븐 패널 위에 떠서 **가로로 거의 꽉 찬 청록 판을 두 줄** 갖는다 —
실측 x100~1340 중 청록 비율 **y2450 = 86~90% · y2730 = 95%** (임계 70%).

문을 **세 겹**으로 잠갔다. 하나라도 안 맞으면 왼쪽(안전한 쪽)이다:
1. `isConfirmDialog` — 청록/주황 배치
2. `isOvenCleanupDialog` — 넓은 청록 판 두 줄
3. **오븐이 도는 중일 때만** — `Oven.kt` 밖에서는 어떤 창에서도 오른쪽을 누르지 않는다

탭 좌표 `OVEN_CLEANUP (990,2812)` 는 실기 화면에서 잰 값이다(PC 봇 쪽 값은 추정치였다).

> **이것이 프로젝트에서 오른쪽 주황을 누르는 유일한 자리다.** 다른 곳에 복사하지 말 것.

#### 상한은 '한 싸이클'에 맞춘다 (2026-09-06)
Auto 한 번이 얼마나 큰지는 **오븐 레벨마다 다르다**(1회 5~50개). 게임의
`자동 열기 → 1회에 여는 개수` 를 설정(⚙ → **오븐 1회 개수**, 기본 20)에 맞춰 두면 그 값으로 상한을 잡는다.
실기 확인: **20개짜리면 한 싸이클로 퀘스트가 끝난다.**

| 상한 | 값 |
|---|---|
| 넘긴 팝업 수 | `1회 개수 + 5` (8~40 사이) |
| 시간 | `max(60초, 1회 개수 × 5초)` |
| **조용해지면 끝** | 팝업도 뱃지 변화도 **10초** 없으면 싸이클이 끝난 것으로 보고 나간다 |

세 번째가 핵심이다 — 예전엔 시간 상한만 있어서 완료 감지가 빗나가면 140초 동안 팝업 55개
(≈장비 110개)를 뽑아 재화를 크게 낭비했다. 이제는 한 싸이클이 끝나면 스스로 멈춘다.

### 보스 소환 — 화면 색으로 판정하지 않는다
스테이지 클리어형 퀘스트는 보스를 깨야 넘어간다. 이때 게임이 **'보스만 눌리는 락'** 을 건다
(빈 곳이나 퀘스트를 눌러도 안 풀리고 보스 버튼만 반응한다). 한 판 들어갔다 나오면 풀린다.

- **버튼이 있는지 색으로 확인하지 않는다.** PC 봇은 빨강 판정(`Test-BossSummonReady`)을 썼다가
  배너 위로 전투 이펙트·데미지 숫자가 겹쳐 값이 흔들려(락 2.09 vs 다른 프레임 0.76) 버튼을
  놓치고 갇혔다. 탐색(뽑기·상자·오븐)이 이미 다 실패한 자리라 바로 눌러도 된다.
- **보스전 중인지도 화면으로 못 읽는다.** 두 번 다 실패했다 — 상단 이름표 자리는 스테이지
  배경에 좌우되고(사막 -13 / 초록 들판 +85), '보스 소환' 버튼 자리는 **보스 HP 바도 같은 빨강**이라
  전투 중에도 값이 똑같다. 그래서 **내가 소환한 시각만 기억해서 35초를 기다린다.**
- 보스가 세서 못 깨면 **쿠키 조합(프리셋) 1~5** 를 하나씩 바꿔 가며 도전한다.
  5개를 다 써도 못 깨면 15분 쉬고 조합 1번부터 다시 한다(그 사이 쿠키가 크면 저절로 넘어간다).

> **보스전 대기를 순서 1.5 에 둔 이유** — PC 봇은 이 검사를 팝업 닫기·보상 뒤에 뒀지만 앞으로 당겼다.
> 전투 중에 뒤로가기를 누르면 '전투를 바로 종료하시겠습니까?' 창이 뜨고, 보상을 받으러 창을
> 여닫으면 전투 중에 화면을 헤집는다. 기다리는 게 맞다.

### ★ 일일 던전 진입 — 배너를 **화면에서 찾아** 누른다 (2026-09-06 실측)
증상: 일일 던전 목록 앞에 서 있으면서 계속 **"가림막 치우는 중"** 으로 되돌아갔다.

원인은 고정 좌표 `DAILY_FIRST (700,830)` 였다. 그 자리 색을 재 보니 `(34,29,26)` —
**배너 사이의 검은 띠**다. 목록이 조금만 스크롤돼 있으면 헛탭이 되고,
`enter()` 는 그걸 '진입 실패'로 보고 `clearPopups` + `resetToMain` 으로 메인까지 되돌아갔다.
목록 앞에 있는데 메인으로 되돌아가니, 다음 바퀴도 같은 자리에서 같은 헛탭을 했다.

**고침 둘:**
1. `Screen.findDailyBanner` — 세로로 훑어 **밝은 구간(배너)** 을 찾아 그 중심을 누른다.
   실측 밝기: 배경 청록 **57~59** · 배너 사이 검은 띠 **29~43** · 배너 **90~223** → 임계 **78** 로 깨끗이 갈린다.
   (이 화면에서 찾은 밴드: y926~1196 · 1778~1958 · 2204~2372)
2. `Screen.atDailyList` — 목록 화면을 알아본다. 하단 서브탭 '일일 던전'이 밝은 청록이고
   그 오른쪽 '쟁탈전'은 까맣다: 실측 `(720,2840)=(3,167,179)` · `(1150,2840)=(0,0,0)`.
   **이미 목록에 있으면 되돌아 나가지 않는다** — 배너만 누르면 되는데 메인까지 갔다 오는 건 낭비다.

> 교훈은 앞서 여러 번 나온 것과 같다 — **목록·격자처럼 스크롤되는 것에 고정 좌표를 박지 말 것.**
> (우측 아이콘 열이 이벤트마다 한 칸씩 밀리던 것, 토벌전 바로가기가 '크럼블 패스'였던 것과 같은 종류다.)

### 일일 던전 — 소탕은 하지 않는다 · 열쇠 없으면 바로 넘어간다 (2026-09-06)
**소탕(SKIP 티켓)을 코드에서 아예 뺐다.** 소탕은 '지금 스테이지' 기준으로 보상을 주는데
스테이지는 계속 올라가므로, 지금 태우면 나중에 받을 것보다 손해다(사용자 판단).
설정 스위치로 두지 않고 지웠다 — **켤 수 있게 두면 언젠가 켜진다.**
좌표(`DAILY_SWEEP*`)는 실측값이라 `Screen.kt` 에 남겼지만 **부르는 곳이 있으면 안 된다.**

**열쇠(기회)가 없으면 바로 다음 던전으로.** 진입 화면인데 '도전'도 '완료'도 아니면 열쇠가 없는
것인데, 예전엔 던전당 상한 **260초를 그냥 기다렸다**(8개면 최악 35분을 버린다).
기다려도 열쇠는 안 생긴다. 지금은 **6초만 지켜보고**(화면 전환 중일 수 있어서) 넘어간다.

### 일일 던전 (`Daily.kt`)
던전 하나씩 돌며 **기회(별티켓)를 다 쓰고 → 달성 보상 → 다음 던전(▶)**. 첫 던전으로
되돌아오면 한 바퀴 돈 것이라 끝낸다(중간 던전끼리는 비슷해서 그걸로 끊으면 일찍 멈춘다).

- **'연속 도전'은 끈다.** 켜면 게임이 봇과 상관없이 다음 판을 이어가 화면이 어긋난다
  (엉뚱한 팝업 위에 탭이 들어간다). 매 판 직접 눌러 '한 판 끝 → 진입 복귀'를 확인하며 나아간다.
- 수령 전에 **진입 화면이 두 번 연속 보일 때까지** 기다린다. 비동기 결과 화면이 남아 있을 수 있다.
  안 잡히면 수령을 건너뛰고 순환만 이어간다(엉뚱한 화면에서 마구 누르지 않는다).
- 던전 구분은 좌상단 **보상 아이콘 박스 9점 지문**으로 한다. 던전마다 색이 뚜렷하고 애니메이션이 없다.
  제목 글자로 하면 안 된다 — 게임 폰트는 OCR 이 못 읽는다.
- `maxDungeons = 0` 으로 부르면 **진입만 시험**한다(입장권을 안 쓴다).

#### ⚠️ PC 좌표가 폰에서 어긋났다 — '던전' 탭
`daily.ps1` · `arena.ps1` 의 `$T_DUNGEON = (530,3054)` 는 **폰에서 아이콘 아래 빈 배경**이다
(실측 `41,31,29`). PC 는 터치 영역이 넓어 우연히 먹었던 것으로 보인다.
아이콘 한가운데인 **`(505,2990)`** 로 옮겼다(실측 크림색 `238,235,230`).

> 하단 네비 좌표를 PC 에서 가져올 때는 **아이콘 한가운데인지 픽셀로 확인할 것.**
> y 가 조금만 아래로 내려가도 배경이다.

**안 옮긴 것**: 소탕(SKIP 티켓). PC 는 `-UseSkip` 스위치로만 켜지는데 폰에는 그 스위치를 둘
자리가 없어 뺐다. 넣으려면 **SKIP 티켓을 쓴다는 걸 화면에 밝혀야 한다.**

### 아레나 (`Arena.kt`)
**나보다 약한 상대만** 골라 싸운다. 상대를 8명 넘겨도 못 찾으면 지금 상대와 그냥 도전한다 —
**크리스탈 새로고침은 절대 안 한다**(300 크리스탈). `maxFights = 0` 이면 진입만 시험한다.

- **승패는 화면으로 못 읽는다** → 아레나 점수의 변화로 가른다(오르면 승·내리면 패, 무승부 없음).
  ±1~2 는 OCR 노이즈라 무시하고, 300 을 넘는 점프는 팝업에 가려 0 으로 읽힌 오독이라 버린다.
- 로비 판정은 **'내 전투력이 읽히면 로비'**. 승급·강등 화면은 위치가 제각각이라 하단만 눌러서는
  못 넘긴다 → 하단 '탭하세요' 와 화면 한가운데를 같이 누른다. 그래도 안 되면 뒤로가기 후 재진입.
- 새로고침 팝업을 만나면 **왼쪽 [취소]**. 오른쪽 주황은 크리스탈 300 이라 어느 창에서든 안 누른다.

#### ⚠️ PC 봇을 그대로 옮기면 안 되는 곳 — 전투력 비교
`arena.ps1` 은 숫자만 뽑아 비교한다. 그러면 `59.64M` 은 `5964`, `1.21G` 는 `121` 이 되어
**단위가 섞이는 순간 더 센 상대를 약하다고 오판한다.** 아레나에서 그건 곧 패배다.
폰에서는 `Ocr.readPower` 가 **단위까지 읽어** 실제 크기로 바꾼다.

    59.64M → 59,640,000    1.21G → 1,210,000,000    59M 70K → 59,070,000    1,234 → 1,234

**다음**
| 할 일 | 검증 비용 |
|---|---|
| (콘텐츠는 다 옮겼다) 실기 검증이 남았다 | — |

### 실기 확인된 기기
| 기기 | 화면 | 게임 영역 | 배율 | |
|---|---|---|---|---|
| 갤럭시 S25 울트라 (SM-S938N) | 1440x3120 | 전체 | 1.000 | 설계 그대로 |
| **갤럭시 탭 S7+ (SM-T970)** | 1752x2800 | **1292x2800 @(230,0)** | **0.897** | 좌우 230px 검은 띠 |

탭 S7+ 는 **세로 게임을 넓은 화면에 넣는 가장 흔한 방식**이었다 — 높이에 맞추고 좌우를 띠로 채운다.
후보 ③(높이 맞춤·가운데 정렬)이 걸렸고, 그 매핑으로 **메인 화면이 실제로 알아보였다**.
계산만 맞은 게 아니라 검증까지 통과한 것이라 그대로 쓴다.

> 진단은 앱의 **점검 → [화면 점검] → [진단 보내기]** 로 글로 뽑는다.

### 진단을 앱이 직접 올리게 하지 못하는 이유 (검토 완료 — 다시 시도하지 말 것)
"사용자가 태블릿에서 보내기만 누르면 내가 바로 읽게" 하려면 두 조건이 다 필요한데 둘 다 막혀 있다.

| 방법 | 막히는 지점 |
|---|---|
| GitHub 이슈·Gist 로 올리기 | 앱에 토큰이 필요하다. **저장소가 공개라 APK 를 뜯으면 누구나 그 토큰을 갖는다** |
| PC 봇이 쓰는 Firebase RTDB 로 올리기 | 앱 쪽은 되지만, 작업 환경의 이그레스 프록시가 `*.firebasedatabase.app` 을 막는다(403). 올려도 내가 못 읽는다 |
| 공개 붙여넣기 서비스 | 같은 이유로 접근이 막혀 있고, 진단이 아무나 읽는 곳에 남는다 |

그래서 **'사람이 한 번 누르는' 경로**로 갔다. `[진단 보내기]` 가 클립보드 복사 **+ 안드로이드 공유창**
(`ACTION_SEND`)을 같이 띄운다 → 채팅 앱을 고르면 그대로 전송된다.
복사→앱 전환→붙여넣기 3동작이 1동작이 됐다. 공유창에 원하는 앱이 없으면 클립보드에 이미 들어 있다.
> 스크린샷을 찍어 옮기는 것보다 훨씬 낫다(사용자가 힘들다고 해서 만들었다).

#### 알아 둘 것 — 켜자마자 뜨는 비율 경고는 부정확하다
관제 화면이 켤 때 `resources.displayMetrics` 로 미리 잡는 값은 **앱 창 크기**라 실제 화면과 다르다
(탭 S7+ 에서 `1752x2698` 로 나왔다 — 시스템 바를 뺀 값). 그래서 시작 전 경고는 참고만 하고,
**진짜 판단은 캡처 한 장을 보고 하는 `Coords.detect`** 가 한다.

### 실기 확인 기록
| 날짜 | 확인한 것 | 결과 |
|---|---|---|
| 09-04 | 토벌전 자동 — 진입·반복 도전·점수 읽기·결과창 정리 (끝까지) | ✅ |
| 09-05 | 보상 받기(미션·출석) — `[보상만 받기]` 한 바퀴 | ✅ |
| 09-05 | **시험 모드로 네 콘텐츠 진입** — 퀘스트·토벌전(길드 경로)·일일 던전·아레나 | ✅ |
| 09-05 | 앱 안 자동 업데이트 · 게임 패키지 자동 탐지 | ✅ |
| 09-06 | **갤럭시 탭 S7+ 좌표 자동 매핑** — 좌우 띠를 걷어내고 메인 화면 인식 | ✅ |

09-05 로 확인된 것:
- **우측 아이콘 열 좌표**(미션 `(1336,1130)` · 달력 `(1336,1300)`) — 제일 의심하던 자리였다.
- **토벌전 길드 경로** — 하단 '길드' 탭 `(905,3000)` → '길드 토벌전' 타일 `(280,1880)`.
- **'던전' 탭을 `(505,2990)` 로 옮긴 판단** — PC 의 `(530,3054)` 는 폰에서 빈 배경이었다.
- 일일 던전·아레나 **진입 화면 판정**(`atDailyEntry` · 아레나는 '내 전투력이 읽히면 로비').

> ⚠️ **진입까지가 확인된 것이지 한 바퀴가 확인된 게 아니다.** 시험 모드는 진입 직후 끝낸다.
> 아래 것들은 아직 한 번도 안 돌아 봤다.

### 아직 실기로 못 본 것
- **퀘스트 순환 한 바퀴** — 띠 수령 · 뽑기 10회 · 가방 상자 · 오븐 · 보스 소환.
  특히 **오븐은 Auto 를 반드시 끄고 나와야 한다**(안 끄면 재화가 샌다). 첫 실행 때 이걸 눈으로 볼 것.
- **일일 던전 한 바퀴** — 전투 반복 · 달성 보상 수령 · `▶` 순환 · 첫 던전 복귀 종료.
- **아레나 실전** — `Ocr.readPower` 가 단위를 제대로 읽는지 로그로 확인할 것.
  `상대 45,200,000 ≤ 나 59,640,000 → 도전` 처럼 **실제 크기**로 찍혀야 한다.
  `5964` 같은 작은 수가 나오면 단위 처리가 깨진 것이다.
- 일일 던전 **소탕**(설정에서 켰을 때만).
