# crumble-phone — 작업 인수인계

쿠키런: 크럼블을 **PC 없이 폰에서** 자동으로 돌리는 안드로이드 앱.
PC용 봇([SOHADA2/crumble-bot](https://github.com/SOHADA2/crumble-bot))의 판정·좌표를 그대로 옮겨 왔다.
**컴퓨터를 옮겨 다니며 작업할 때 이 문서가 기준이다. 새 PC에서는 여기부터 읽는다.**

---

## 1. 왜 좌표를 그대로 쓸 수 있나

대상 폰이 **갤럭시 S25 울트라(SM-S938N) · 안드로이드 16 · 1440x3120** 이고,
이건 PC 봇의 기준 해상도(LDPlayer 1440x3120)와 **완전히 같다.** 그래서 좌표 변환이 없다.

> 다른 기기로 옮기면 이 전제가 깨진다. 1080x2340 · 720x1560 은 **비율이 같아** 배율만 곱하면 되지만,
> 태블릿(4:3·16:10)은 게임이 UI를 재배치하므로 앵커 기반 매핑이 따로 필요하다.

## 2. 새 PC 준비

```powershell
git clone https://github.com/SOHADA2/crumble-phone.git
cd crumble-phone
powershell -ExecutionPolicy Bypass -File setup-dev.ps1        # 기본 D:\android-dev
.\dev.ps1 install                                             # 빌드 + 폰에 설치 + 권한 복구
```

`setup-dev.ps1` 이 JDK17 · Android SDK(플랫폼 34) · Gradle 을 **한 폴더에** 받는다(시스템은 안 건드림).
Android Studio 는 필요 없다.

| 명령 | 하는 일 |
|---|---|
| `.\dev.ps1 build` | APK 만 빌드 |
| `.\dev.ps1 install` | 빌드 + 설치 + 접근성/오버레이 권한 복구 + 앱 실행 |
| `.\dev.ps1 log` | 앱 기록 30줄 |
| `.\dev.ps1 shot` | 폰 화면 캡처를 `shots\` 에 저장 |

### ⚠️ 재설치할 때마다 사람이 눌러야 하는 것 하나
**화면 읽기(MediaProjection) 동의**는 세션 단위라 재설치하면 초기화된다.
앱에서 `[화면 읽기 허용]` → **"앱 하나 공유"를 "전체 화면"으로 바꾸고** → `[다음]` → `[지금 시작]`.
("앱 하나 공유"로 두면 우리 앱 자신만 찍혀서 게임을 못 읽는다.)
접근성·오버레이 권한은 `dev.ps1` 이 adb 로 되살리므로 다시 안 물어본다.
→ **빌드 횟수를 아껴서 여러 수정을 한 번에 묶을 것.**

## 3. 배포와 앱 안 자동 업데이트

`main` 에 push 하면 **GitHub Actions 가 APK 를 대신 빌드**해 `latest` 릴리스에 붙인다
(`app-debug.apk` 와 `latest.json`). 개발 PC 가 없어도(클라우드에서 작업해도) 폰에 넣을 APK 가 나온다.
**폰에 붙어서 하는 실기 검증만** adb 가 연결된 PC 가 필요하다.

### ⏳ 앱 안 [업데이트] 는 아직 안 산다 — 마지막 한 칸이 남았다
앱 코드는 다 됐다. **저장소가 비공개라 릴리스 주소가 로그인을 요구해서** 앱이 못 받을 뿐이다.
지금은 업데이트 확인이 조용히 실패하고 넘어간다(사용자에게 아무 말도 안 한다).

앱에 토큰을 넣는 건 답이 아니다 — **APK 를 뜯으면 누구나 저장소에 접근하게 된다.** 하지 말 것.

끝내는 길은 둘이고, **어느 쪽이든 코드 한 줄 + 워크플로 한 단계**다.

| | 할 일 | 코드 |
|---|---|---|
| **A. 이 저장소를 공개로** | Settings → Danger Zone → Change visibility | **이미 이 상태에 맞춰져 있다.** 공개로 바꾸는 순간 바로 동작한다 |
| **B. 공개 릴리스 저장소 분리** | ① 공개 저장소 `crumble-phone-releases` 생성 ② 그 저장소에 Contents:RW 토큰 발급 ③ 이 저장소 시크릿 `RELEASES_TOKEN` 에 등록 | `Updater.BASE` 를 그 저장소로 바꾸고, 워크플로에 그 저장소로 올리는 `softprops/action-gh-release` 단계를 하나 더 둔다(`repository:` + `token:`) |

A 는 소스가 공개된다. B 는 소스가 비공개로 남지만 준비가 세 단계다.
**B 로 갈 예정이다(PC 에서 작업할 때 저장소를 따로 만들기로 했다).**

> GitHub 은 **토큰 발급에 API 가 없다.** 계정 주인이 브라우저에서 직접 만들어야 한다 —
> 자동화로 대신 해 줄 수 없는 유일한 단계다. 저장소 생성도 이 세션의 GitHub 연결로는 403 이 났다.

### 앱 안 [업데이트]
켤 때 조용히 한 번 확인하고, 새 판이 있으면 줄에 `새 버전 1.12 있음` 이 파랗게 뜬다.
누르면 **받아서 설치 화면까지 띄운다.** 마지막 '설치' 확인은 안드로이드가 반드시 사용자에게 묻는다
(사이드로드 앱은 예외가 없다 — 기기 관리자 앱만 건너뛸 수 있다).

`Updater.kt` 가 릴리스의 `latest.json` 을 읽어 `versionCode` 를 지금 판과 비교하고,
APK 를 받아 `PackageInstaller` 세션으로 넘긴다(FileProvider 가 필요 없다).

> ⚠️ 주소는 **태그를 직접** 가리켜야 한다(`/releases/download/latest/...`).
> `/releases/latest` 는 프리릴리스를 건너뛰어서 404 가 난다.

### ★ 서명 키를 저장소에 고정해 뒀다 — 이게 없으면 업데이트가 성립하지 않는다
`app/build.gradle.kts` 가 `debug.keystore` 를 명시적으로 쓴다.
**이 설정이 없으면 AGP 가 빌드할 때마다 `~/.android/debug.keystore` 를 새로 만드는데,
CI 러너는 매번 새 기계라 매 빌드 서명이 달라진다.** 서명이 다르면 안드로이드가 덮어쓰기 설치를
거부한다("앱이 설치되지 않았습니다"). debug 키라 비밀번호는 안드로이드 관례값(`android`) 그대로다.

> ⚠️ **키를 박기 전(v1.10 이전)에 깔았던 판이 폰에 있으면 한 번은 지우고 새로 깔아야 한다.**
> 그때까지는 빌드마다 서명이 달랐다. 한 번 지우고 깔면 그다음부터는 계속 덮어쓰기가 된다.

버전은 `versionCode = GitHub Actions 실행 번호`, `versionName = 1.<번호>` 다. 계속 커지므로
앱이 새 판인지 숫자만 비교하면 된다.

### 처음 설치할 때 (폰에 앱이 아직 없을 때)
폰 브라우저로 아래를 열어 `app-debug.apk` 를 받는다. 그다음부터는 앱 안 [업데이트] 로 끝난다.

    https://github.com/SOHADA2/crumble-phone/releases/tag/latest

## 4. 구조

| 파일 | 역할 | PC 봇에서 대응 |
|---|---|---|
| `TapService.kt` | 접근성 서비스 — 탭·드래그·뒤로가기 | `adb shell input tap/swipe/keyevent 4` |
| `CaptureService.kt` | MediaProjection 포그라운드 서비스 — 화면 읽기 | `adb exec-out screencap` |
| `Screen.kt` | 화면 판정(메인·확인창·로비·결과창)과 좌표 | `dev.ps1` + `tobol.ps1` |
| `Runner.kt` | 콘텐츠 루프(현재 토벌전) | `tobol.ps1` |
| `Ocr.kt` | 숫자 읽기(ML Kit) | `ocr.ps1` |
| `Overlay.kt` | 게임 위 상태 알약 | 관제창 |
| `MainActivity.kt` | 관제 화면 | `CrumbleBotWpf.ps1` |

### 화면 읽기를 끄는 길 (없으면 못 끈다)
MediaProjection 은 **포그라운드 서비스**라 앱을 최근목록에서 밀어내도 안 죽는다.
끄는 길을 안 만들면 '화면을 읽는 중' 알림이 계속 남는다(실제로 그랬다). 지금은 세 군데다.

| 끄는 곳 | 경로 |
|---|---|
| 알림의 `[끄기]` | `PendingIntent.getForegroundService` → `ACTION_STOP` |
| 관제 화면 `[화면 읽기 끄기]` | `CaptureService.stop(ctx)` — 켜져 있을 때만 보인다 |
| 상태바에서 공유 끊기 | `MediaProjection.Callback.onStop` |

셋 다 같은 `shutdown()` 으로 모인다 — **돌던 것 멈춤 → 오버레이 걷기 → 캡처 놓기 → 알림 제거**.

> ⚠️ `START_STICKY` 를 쓰면 안 된다. 시스템이 서비스를 되살려도 화면 읽기 동의는 **1회용 토큰**이라
> 같이 살아나지 않는다. 그래서 캡처는 안 되는데 알림만 남고, 꺼도 다시 뜨는 것처럼 보인다.
> `START_NOT_STICKY` 로 둔다.
> 같은 이유로 `onStop` 콜백에서 `instance` 를 반드시 비워야 한다. 안 그러면 앱은 '읽는 중'인 줄 알고
> 영영 빈 화면을 붙든다.

### ⚠️ 게임 패키지를 하나로 박으면 안 된다 (`GameApp.kt`)
같은 게임이라도 **스토어마다 패키지가 다르다.** 구글 플레이로 깔았다가 **갤럭시 스토어**로 다시 깔면
앱이 게임을 못 알아본다(실제로 겪었다 — `게임을 찾지 못했어요`).

- `GameApp` 이 런처에 뜨는 앱 중 **`com.devsisters.` 로 시작하는 것**을 찾아 `Prefs.gamePackage` 에 기억한다.
- 자동 탐지가 빗나가면 **도구 → 게임 앱** 에서 손으로 고른다.
- 매니페스트 `<queries>` 에 `MAIN/LAUNCHER` 인텐트를 넣어 런처 앱을 볼 수 있게 했다.
  `QUERY_ALL_PACKAGES` 는 안 쓴다 — 이걸로 충분하고 권한이 더 얌전하다.

> **화면 판정·좌표는 스토어와 무관하게 같다.** 패키지만 맞으면 나머지는 그대로 돈다.
> 접근성 서비스도 `packageNames` 로 제한하지 않았으므로 어느 판이든 탭이 들어간다.

### 안드로이드 규칙 메모
- `getMediaProjection()` **전에** `foregroundServiceType=mediaProjection` 으로 포그라운드 전환이 끝나 있어야 한다(14+).
- 매니페스트 `<queries>` 에 `com.devsisters.cc` 가 없으면 `getLaunchIntentForPackage` 가 null 을 준다(11+).
- 게임이 스스로 `KEEP_SCREEN_ON` 을 걸어서 화면은 안 꺼진다. 대신 폰이 전용으로 묶이고 발열·배터리를 감수해야 한다.

## 5. 관제 UI 방침

**이미지 파일도 커스텀 글꼴도 쓰지 않는다.** 이건 그대로다 — APK 를 가볍게 유지하고,
새 파일이 안 생기니 배포에서 신경 쓸 것도 없다. 대신 *"공식 앱처럼"* 을 이렇게 낸다.

### 구조는 iOS 의 '묶음 목록(Inset Grouped)'
```
큰 제목 → 지금 상태 카드 → (도는 중이면 멈추기) → 구역별 묶음 목록 → 기록
```
- **콘텐츠는 콘텐츠끼리 한 목록에.** '자동 실행' 에 `퀘스트` · `토벌전` · `보상만 받기` 가 나란히
  있고, 각 줄에 부제로 무엇을 하는지 적는다. 일일 던전·아레나가 붙어도 **줄 하나만 늘면 된다.**
  > 처음엔 `[봇 본체 시작]` 만 주 버튼으로 크게 뒀는데, 그러면 콘텐츠가 늘 때 구조가 깨진다.
  > '봇 본체' 라는 이름도 PC 봇 용어라 화면에서는 뜻이 안 통했다 → **`퀘스트`** 로 바꿨다
  > (코드 파일은 `Chores.kt` 그대로다).
- **주 버튼은 돌고 있을 때만 나온다** — 빨간 `[멈추기]` 하나. 쉴 때는 화면에 버튼이 없고
  목록에서 고른다. 한 번에 하나만 돌므로 도는 동안 나머지 줄은 흐려진다.
- 묶음은 `radius 12` 카드 하나에 셀을 넣고 **모서리 클리핑은 카드가 한 번만** 한다.
  셀마다 둥글리면 iOS 처럼 안 보인다.
- 셀 사이 선은 `0.5dp`, 왼쪽을 `16dp` 들여쓴다.
- '준비' 구역은 셋 다 되면 통째로 사라진다. 할 일이 없으면 화면에서도 없어야 한다.

### 위계는 글자 크기로 낸다 (색을 늘리지 말 것)
| 크기 | 무게 | 쓰는 곳 |
|---|---|---|
| 34 | Bold | 큰 제목 |
| 22 | Medium | 지금 하는 일 |
| 17 | Regular | 목록 줄 · 버튼 |
| 13 | Regular | 목록 줄의 부제 |
| 15 | Regular | 설명 |
| 13 | Medium | 구역 이름 · 상태 꼬리표 |

### 색은 iOS 시스템 색을 그대로 가져왔다 (`Theme.kt`)
손으로 고른 색보다 서로 잘 맞고 **라이트/다크가 이미 짝지어져 있다.**
`Theme.of(ctx)` 가 시스템 설정을 보고 한 벌을 고른다 — 화면에 색을 직접 적지 말고 여기서 꺼내 쓴다.

| 역할 | 다크 | 라이트 |
|---|---|---|
| 바탕 | `#000000` | `#F2F2F7` |
| 카드 | `#1C1C1E` | `#FFFFFF` |
| 선 | `#38383A` | `#C6C6C8` |
| 글씨 / 보조 / 흐림 | `#FFFFFF` `#98989E` `#6C6C70` | `#000000` `#6C6C70` `#A0A0A5` |
| 파랑 / 빨강 / 초록 / 주황 | `#0A84FF` `#FF453A` `#30D158` `#FF9F0A` | `#007AFF` `#FF3B30` `#34C759` `#FF9500` |

### 아이콘도 벡터로 그렸다
- 런처: `mipmap-anydpi-v26/ic_launcher.xml` (적응형) + `drawable/ic_launcher_*.xml`.
  갈색 바탕 `#C2703A` 에 크림색 쿠키. **minSdk 26 이라 적응형만 있어도 모든 기기가 쓴다**
  (PNG 를 여러 해상도로 넣을 필요가 없다). `monochrome` 도 넣어 테마 아이콘까지 지원한다.
- 알림줄: `drawable/ic_stat_bot.xml`. 시스템이 흰 실루엣으로만 그리므로 단색 한 덩어리다.
  전에 쓰던 `android.R.drawable.ic_menu_view` 는 돋보기라 무슨 앱인지 알 수 없었다.

### 진행률 막대
`Runner.progress` (0~100, **-1 이면 숨김**) 하나만 보고 상태 카드가 그린다.
`Runner.set()` 으로 새 상태를 쓰면 자동으로 -1 이 된다 — 다음 단계로 넘어갔으니 앞 막대는 의미가 없다.

> **진짜 분모가 있을 때만 채운다.** 없는 걸 지어내면 막대가 거짓말을 한다.

| 작업 | 분모 |
|---|---|
| 오븐 | 넘긴 팝업 / 15회 |
| 보스전 대기 | 지난 시간 / 35초 |
| 뽑기 결과 정리 | 닫은 횟수 / 24회 |
| 보상 받기 | 걸음 / 6 (미션 3탭 + 출석 3단계) |
| 토벌전 전투 | 지난 시간 / 90초 — **'포기까지 얼마나 남았나'** 다. 전투가 언제 끝날지는 모른다 |
| 그 밖(퀘스트 대기 등) | 없음 → 막대를 숨긴다 |

너비를 직접 바꾸면 애니메이션이 안 된다. 꽉 찬 막대를 왼쪽 기준(`pivotX=0`)으로
`scaleX` 만 줄여 두면 부드럽게 움직인다.

### 첫 실행 안내 (`SetupActivity.kt`)
켤 것이 남아 있으면 앱을 켤 때 안내가 먼저 뜬다(한 번만 강요한다. 목록의
`차근차근 안내 받기` 로 언제든 다시 연다).

**할 일의 개수는 줄일 수 없다** — 접근성도 화면 읽기도 안드로이드가 사용자에게 직접 받는
허가라 앱이 대신 켤 방법이 없다. 대신 **막히는 지점**을 없앤다.
- 지금 할 것 **하나만** 펼치고, 끝난 것은 접어서 ✓ 로 바꾼다. 1초마다 다시 보므로
  설정 화면에 다녀오면 알아서 다음 칸으로 넘어간다.
- 각 단계에서 해당 설정 화면으로 바로 보낸다. 접근성은 `:settings:fragment_args_key` 로
  우리 항목을 짚어서 연다(안 되는 기기는 그냥 목록이 열린다).

여기서 **실제로 넘어지는 두 곳**을 미리 말해 준다. 둘 다 사용자가 직접 겪은 것이다.

| 넘어지는 곳 | 안내 |
|---|---|
| 접근성 스위치가 **회색** | 안드로이드 13+ 는 스토어 밖 설치 앱의 접근성을 막는다 → 앱 정보 ⋮ → **제한된 설정 허용**. 그 화면으로 보내는 버튼을 같이 둔다 |
| 화면 읽기 동의의 **'앱 하나 공유'** | 그대로 두면 우리 앱 자신만 찍혀 게임을 못 본다 → **'전체 화면'** 으로 바꾸라고 노란 칸으로 경고 |

> 노란 경고 칸은 **여기서 실제로 넘어지는 자리에만** 쓴다. 늘려 놓으면 아무도 안 읽는다.

### 폰마다 한 번은 해야 하는 것 (없앨 수 없다)
| 단계 | 왜 |
|---|---|
| 브라우저에 '출처를 알 수 없는 앱' 허용 | 스토어 밖 설치는 무조건 |
| Play 프로텍트 차단 넘기기 | 접근성+설치 권한 조합이라 자동 차단된다. Play 스토어 → 프로필 → Play 프로텍트 → ⚙ → 검사 끄기 → 설치 → 다시 켜기 |
| 제한된 설정 허용 | 안드로이드 13+ |
| 접근성 서비스 켜기 | 탭을 넣는 핵심. 시스템 설정에서만 켤 수 있다 |

화면 읽기 동의는 **1회용 토큰**이라 앱이 저장할 수 없다. 다만 `CaptureService` 가 살아 있는
동안은 유효하므로, 껐다 켜거나 재부팅했을 때만 다시 누르면 된다.
PC 가 있으면 `dev.ps1 install` 이 접근성·오버레이를 adb 로 부여해 대부분을 건너뛴다.

### 설정 (`Prefs.kt`)
SharedPreferences 하나. 콘텐츠 코드가 Context 를 들고 다니지 않게 `Prefs.init(ctx)` 한 번 하고
어디서든 값만 읽는다. **init 전에 읽어도 죽지 않고 기본값이 나온다**(nullable 로 둔 이유).

| 설정 | 기본 | 하는 일 |
|---|---|---|
| **시험 모드** | 꺼짐 | **재화·입장권을 하나도 안 쓰고 진입까지만.** 좌표를 공짜로 확인하는 길 |
| 아레나 판수 | 10 | 5 · 10 · 20 · 30 중 탭할 때마다 돈다. 재화가 먼저 떨어지면 거기서 끝 |
| 일일 던전 소탕 | 꺼짐 | 기회를 다 쓴 뒤 **SKIP 티켓까지** 쓴다(광고 제거 보유자용) |

> **시험 모드는 그냥 편의 기능이 아니다.** `maxDungeons=0` · `maxFights=0` 같은 안전한 시험 방법을
> 코드에 넣어 놓고 **누를 방법을 안 만들면 아무 소용이 없다.** 실제로 그 상태로 한 판 나갔다.
> 각 콘텐츠는 이렇게 받는다 — 일일 던전 `0`, 아레나 `0`, 토벌전 `0`, 퀘스트는 **오븐만 건너뛴다**
> (퀘스트 순환에서 재화를 쓰는 건 오븐뿐이다).

### 무엇이 도는지 보이게 — 알림줄이 주력이다
봇이 도는 동안 사용자는 **게임 화면**을 본다. 앱을 다시 열어야만 진행을 알 수 있으면 소용이 없다.
그래서 상태를 세 군데에 같이 낸다. 셋 다 `Runner.task` / `status` / `detail` / `progress` 하나만 본다.

| 자리 | 언제 보이나 | 내용 |
|---|---|---|
| **알림줄** (`CaptureService`) | **항상** — 권한이 따로 필요 없다 | `크럼블 폰봇 · 퀘스트` / `오븐에서 장비 뽑는 중 · 7/15` + 진행 막대 |
| 게임 위 알약 (`Overlay`) | 오버레이 권한이 있을 때 | `퀘스트 · 오븐에서 장비 뽑는 중 47% · 7/15` |
| 관제 화면 상태 카드 | 앱을 열었을 때 | `● 퀘스트 도는 중` + 큰 글씨 + 막대 |

> **알림줄이 주력이다.** 오버레이는 권한이 있어야 뜨지만 알림은 항상 뜬다. 예전엔 여기가
> `화면을 읽는 중` 으로 고정돼 있어서 제일 좋은 자리를 놀리고 있었다.

- 글이 **바뀔 때만** 다시 그린다(같은 글로 계속 `notify` 하면 시스템이 눌러 버린다).
- `setOnlyAlertOnce(true)` — 글이 바뀔 때마다 소리·진동이 나면 안 된다.
- 알림을 누르면 관제 화면이 열린다. `[끄기]` 는 그대로 화면 읽기를 끈다.

### 게임 위 알약이 사라지던 두 가지 (2026-09-06 수정)
증상: **어떨 때는 떠 있고 어떨 때는 없다.**

1. **`idleTicks` 를 `show()` 에서 안 되돌렸다** ← 주범.
   `show()` 는 콘텐츠를 시작하기 **전에** 불리므로 첫 tick 때 아직 `Runner.running` 이 false 다.
   지난 번에 쉬면서 7 까지 세어 둔 값이 남아 있으면 그 한 번으로 임계(6)를 넘어 **바로 hide** 된다.
   → 첫 실행만 되고 그 뒤로는 안 뜬다. `show()` 에서 `idleTicks = 0`.
2. **관제 화면에서만 띄웠다.** 봇은 서비스로 계속 도는데 앱을 최근목록에서 밀어내면
   알약만 사라진다. → `CaptureService` 의 1초 ticker 가 `Overlay.ensure()` 로 보장한다
   (서비스는 앱이 닫혀도 살아 있고, 그 핸들러는 메인 스레드라 `addView` 가 안전하다).

> `WindowManager.LayoutParams` 의 `x`/`y` 는 **픽셀**이다. 안전 띠 `Y_MIN`~`Y_MAX` 도 픽셀이라
> 초기 위치를 `dp()` 로 주면 자가 섞인다. 지금은 둘 다 픽셀로 맞췄다.

### 게임 위 알약(`Overlay.kt`)만 예외
게임 위에 뜨는 것이라 **관제 화면의 라이트/다크와 무관하게 언제나 어두운 반투명**으로 둔다.
게임이 밝든 어둡든 읽혀야 하기 때문이다. 모서리는 `radius 100` 으로 완전한 알약.

## 6. 실측값 — 다시 재지 말 것

| 대상 | 값 |
|---|---|
| 메인 판정 | 독(300~1150, 2820~2980) 나무색 비율 ≥0.1 **AND** 하단 중앙 주황 닫기버튼 없음(7점 중 4개 미만) |
| 확인창 | 왼쪽(330~540, 2780~2845) 청록 + 오른쪽(890~1100) 주황이 **동시에** → 왼쪽(441,2797)이 언제나 안전한 쪽 |
| 토벌 로비 | (700,2700) 주황 |
| 결과창 | (100,1600)·(1340,1600) 어두운 갈색 **AND** 하단 주황 ✕ |
| 최종 점수 | 숫자는 x400~1070 · y1400~1506 → OCR 영역 **(330,1380,830,150)**, 6자리 이상만 인정 |
| 도전하기 / 결과창 ✕ | (700,2715) / (715,2990) |
| 절전 해제 | (721,1864) → (725,2520) 드래그 |
| 오버레이 안전 띠 | **y120~900** — 판정 지점도 탭 자리도 없는 구간 |

## 6.5 토벌전 진입 — 길드 탭이 먼저다 (2026-09-05)

우측 아이콘 열은 이벤트에 따라 밀려서 실제로 사고가 났다(7-1 참고). **하단 네비는 화면이 바뀌어도
자리가 그대로라** 이쪽이 훨씬 안전하다. 그래서 길을 둘로 두고 길드 탭을 먼저 쓴다.

```
1) 하단 '길드' 탭 (905,3000) → 길드 화면 → '길드 토벌전' 타일 (280,1880)   ← 주 경로
2) 메인 우측 아이콘 열의 피냐타 바로가기 (Shortcut.findTobol)              ← 폴백
```
2)는 이미 실기에서 되는 걸 확인한 길이라 남겨 뒀다. 1)이 막히면 받아 준다.

> **길드 화면인지 확인한 뒤에만 타일을 누른다.** 아니면 아무것도 누르지 않고 물러난다.
> 화면을 안 보고 좌표를 누르는 짓은 하지 않는다 — 그 자리는 화면마다 다른 것이 있다.

### 실측 (사용자 스크린샷 3장, 이미 1440x3120 이라 그대로 읽었다)
| 판정 | 메인 | 길드 | 토벌 로비 |
|---|---|---|---|
| 청록 바탕 4점 `(60,1500) (1380,1500) (60,2700) (1390,2750)` | 0/4 | **4/4** | 4/4 |
| 타일 자리 `(200,1800)-(400,1980)` 평균 밝기 | 42.6 | **162.5** | 94.9 |
| 도전하기 `(700,2700)` | (255,255,66) | (8,38,48) | **(253,149,0)** |

- 청록 판정: `B > R*2 && B > G && B > 40` 이 3점 이상.
- `atGuild` = 청록 바탕 **그리고** 타일 밝기 ≥ 130. 청록만 보면 던전·로비도 걸린다.
- 하단 주황 ✕ 7점은 길드 7/7 · 로비 5/7 · 메인 0/7 로, 기존 서브 화면 판정과도 들어맞았다.

### 같이 고친 것 — 로비 판정이 아슬아슬했다
`atTobolLobby` 는 `(700,2700)` 이 주황이면 로비로 봤는데, **메인 화면의 같은 자리는 골드 숫자라
노랑 `(255,255,66)`** 이다. `B < 60` 을 **6 차이로** 겨우 비켜가고 있었다 — 화면이 조금만 밝아졌으면
메인을 로비로 오판했다. `G` 를 `111..199` 로 가둬 벌려 놨다(주황 G≈149 / 노랑 G=255).

## 7. 함정 모음 (전부 실제로 당한 것)

1. **우측 아이콘 열은 고정 좌표로 누르면 안 된다.** 이벤트에 따라 한 칸(150px)씩 밀린다.
   PC 기준 토벌전 바로가기 `(1340,1500)` 이 폰에서는 **'크럼블 패스'(구매 화면)** 였다.
   → 지금은 길드 탭이 주 경로다(6.5). `Shortcut.findTobol` 은 폴백으로 남아 있다.
2. **로딩 중에 판정하면 안 된다.** 고정 대기로 자르면 로딩 화면에서 뒤로가기를 눌러 '전투 중'으로 잘못 끝난다.
   → `waitGameReady()` 가 아는 화면이 보일 때까지 최대 45초 기다린다.
3. **결과창을 '어둡기'만으로 판정하면 로딩 화면을 결과창으로 오인한다.** → 주황 ✕ 를 함께 본다.
4. **점수 OCR 영역은 실측해야 한다.** PC 영역을 그대로 쓰면 숫자 아래가 잘려 `204,721` 처럼 오독한다.
5. **탭 시험은 게임을 먼저 띄우고**. 앱이 앞에 있으면 앱 자신을 누른다.
6. `local.properties` 의 `sdk.dir` 는 역슬래시가 이스케이프된다 → 슬래시로 쓸 것.
7. PowerShell 에서 `adb exec-out screencap -p > file` 은 PNG 를 깨뜨린다 → `shell screencap` + `pull`.
8. PowerShell 5.1 은 네이티브 exe 의 stderr 를 `2>&1` 로 받으면 죽는다(`ErrorActionPreference='Stop'` 과 함께면 특히).

## 8. 지금까지 된 것 / 다음

**된 것**
- **토벌전 자동** — 진입·반복 도전·점수 읽기(ML Kit)·결과창 정리
- **봇 본체**(`Chores.kt`) — 보상 자동 받기(미션·출석) · 완료 퀘스트 수령 ·
  쿠키 뽑기 10회 · 가방 상자 사용 · 오븐 장비 뽑기(`Oven.kt`) · **보스 소환**
  → PC 봇 `bot.ps1` 의 퀘스트 순환이 폰에서 통째로 돈다
- 게임 위 상태 알약 · 관제 화면 · `[새 APK 받기]` 버튼(릴리스 페이지 열기)

### 봇 본체가 도는 방식 (`Chores.loop`)
매 바퀴 화면을 한 장 읽고 위에서부터 걸러 낸다. 순서가 곧 안전장치다.

| 순서 | 판정 | 하는 일 |
|---|---|---|
| 1 | 독 `cv < 0.4` (화면이 평평) | 왕관 씌우기 2번 → 안 풀리면 뒤로가기·하단X·바깥 순으로 팝업 닫기 |
| 1.5 | 내가 소환한 보스전이 도는 중 | 아무것도 안 누르고 기다린다 |
| 2 | 메인 + 20분 지남 | 미션 '모두 받기' · 출석 '받기' |
| 3 | 메인이 아님 | 뒤로가기를 주력으로 단계적 닫기(공지 X 자리는 제각각이라 좌표 고정 금지) |
| 4 | 퀘스트 띠 `< -0.6` (덮임) | 바깥 → 하단X → 결과창X → 뒤로가기 순으로 치우기 |
| 5 | 퀘스트 띠 `≥ 0.85` (완료) | 띠를 눌러 보상 수령 |
| 6 | 그 외 | 띠를 눌러 유형 확인 → 뽑기 화면이면 10회 / 가방이면 상자 / 그대로면 오븐 |
| 7 | 6도 실패 | 스테이지 클리어형으로 보고 **보스 소환** (조합 1~5) |

> **1이 2보다 먼저인 이유**: 절전 화면에서 좌표를 누르면 아무 일도 안 일어나는데 봇은 했다고 여긴다.
> **4가 5보다 먼저인 이유**: 팝업이 띠를 덮으면 띠 비율이 음수로 튀는데, 이걸 안 거르면
> 5의 임계(0.85)에 안 걸려 '미완료'로 보고 계속 헛탐색한다.

### 오븐 (`Oven.kt`) — Auto 를 끄는 게 제일 중요하다
Auto → '자동 열기' → [시작] → 알아서 뽑힌다. 장착/판매 팝업은 **버튼 말고 바깥**을 눌러 넘긴다
(Auto 가동 중에는 이렇게 넘겨도 퀘스트 수치가 오르고 장비는 오븐에 쌓인다).

> ★ **Auto 는 어떤 경로로 끝나든 반드시 꺼야 한다.** 켜 둔 채 나가면 재화를 계속 소모한다.
> 그래서 시작에 성공한 뒤로는 전부 `try/finally` 안에서 돈다 — 사용자가 [멈추기] 를 눌러도
> 끄는 것까지는 하고 나간다.

- 한 번에 몇 개 나오는지는 **오븐 레벨마다 다르다.** 개수에 의존하지 않고 '넘긴 팝업 수'와
  '퀘스트 완료'로만 판단하고, 상한 두 개(시간·15회)로 폭주를 막는다.
  (예전에 시간 상한만 두고 완료 감지가 빗나가 140초에 팝업 55개를 뽑아 재화를 크게 낭비했다.)
- 가동 중 완료 판정 문턱은 **1.0** 이다(평소 0.85 가 아니라). '자동 열기' 패널이 뜨는 순간
  띠 영역이 0.85~0.88 로 읽혀 **0회 뽑고 헛완료**로 오판하던 사고가 있었다. 진짜 금색은 1.1+ 다.
- 스테이지 클리어형 퀘스트도 화면이 안 바뀌어 오븐과 구분이 안 된다 → **퀘스트당 한 번만** 돌려 본다.

#### ⚠️ PC 봇과 일부러 다르게 한 것: 꽉 찬 오븐의 '자동 정리' 확인창
`oven.ps1` 은 이때 **오른쪽 주황 [정리하기]** 를 눌렀다. 폰에서는 **왼쪽(안전한 쪽)으로 닫고 물러난다.**
- 확인창은 전투 종료·게임 종료·다이아 새로고침이 **전부 같은 배치**라 색만으로 못 가른다.
  잘못 누르면 전투 강제 종료 / 게임 종료 / 크리스탈 300 소모다.
- PC 쪽 [정리하기] 좌표부터가 스크린샷 기준 **추정치**라 실측된 적이 없다.
- 오븐 퀘스트 하나를 건너뛰는 손해가 훨씬 싸다. 오븐이 꽉 차면 **한 번 손으로 정리**해 주면 된다.

### 보스 소환 — 화면 색으로 판정하지 않는다
스테이지 클리어형 퀘스트는 보스를 깨야 넘어간다. 이때 게임이 **'보스만 눌리는 락'** 을 건다
(빈 곳이나 퀘스트를 눌러도 안 풀리고 보스 버튼만 반응한다). 한 판 들어갔다 나오면 풀린다.

- **버튼이 있는지 색으로 확인하지 않는다.** PC 봇은 빨강 판정(`Test-BossSummonReady`)을 썼다가
  배너 위로 전투 이펙트·데미지 숫자가 겹쳐 값이 흔들려(락 2.09 vs 다른 프레임 0.76) 버튼을
  놓치고 갇혔다. 탐색(뽑기·상자·오븐)이 이미 다 실패한 자리라 바로 눌러도 된다.
- **보스전 중인지도 화면으로 못 읽는다.** 두 번 다 실패했다 — 상단 이름표 자리는 스테이지
  배경에 좌우되고(사막 -13 / 초록 들판 +85), '보스 소환' 버튼 자리는 **보스 HP 바도 같은 빨강**이라
  전투 중에도 값이 똑같다. 그래서 **내가 소환한 시각만 기억해서 35초를 기다린다.**
- 보스가 세서 못 깨면 **쿠키 조합(프리셋) 1~5** 를 하나씩 바꿔 가며 도전한다.
  5개를 다 써도 못 깨면 15분 쉬고 조합 1번부터 다시 한다(그 사이 쿠키가 크면 저절로 넘어간다).

> **보스전 대기를 순서 1.5 에 둔 이유** — PC 봇은 이 검사를 팝업 닫기·보상 뒤에 뒀지만 앞으로 당겼다.
> 전투 중에 뒤로가기를 누르면 '전투를 바로 종료하시겠습니까?' 창이 뜨고, 보상을 받으러 창을
> 여닫으면 전투 중에 화면을 헤집는다. 기다리는 게 맞다.

### 일일 던전 (`Daily.kt`)
던전 하나씩 돌며 **기회(별티켓)를 다 쓰고 → 달성 보상 → 다음 던전(▶)**. 첫 던전으로
되돌아오면 한 바퀴 돈 것이라 끝낸다(중간 던전끼리는 비슷해서 그걸로 끊으면 일찍 멈춘다).

- **'연속 도전'은 끈다.** 켜면 게임이 봇과 상관없이 다음 판을 이어가 화면이 어긋난다
  (엉뚱한 팝업 위에 탭이 들어간다). 매 판 직접 눌러 '한 판 끝 → 진입 복귀'를 확인하며 나아간다.
- 수령 전에 **진입 화면이 두 번 연속 보일 때까지** 기다린다. 비동기 결과 화면이 남아 있을 수 있다.
  안 잡히면 수령을 건너뛰고 순환만 이어간다(엉뚱한 화면에서 마구 누르지 않는다).
- 던전 구분은 좌상단 **보상 아이콘 박스 9점 지문**으로 한다. 던전마다 색이 뚜렷하고 애니메이션이 없다.
  제목 글자로 하면 안 된다 — 게임 폰트는 OCR 이 못 읽는다.
- `maxDungeons = 0` 으로 부르면 **진입만 시험**한다(입장권을 안 쓴다).

#### ⚠️ PC 좌표가 폰에서 어긋났다 — '던전' 탭
`daily.ps1` · `arena.ps1` 의 `$T_DUNGEON = (530,3054)` 는 **폰에서 아이콘 아래 빈 배경**이다
(실측 `41,31,29`). PC 는 터치 영역이 넓어 우연히 먹었던 것으로 보인다.
아이콘 한가운데인 **`(505,2990)`** 로 옮겼다(실측 크림색 `238,235,230`).

> 하단 네비 좌표를 PC 에서 가져올 때는 **아이콘 한가운데인지 픽셀로 확인할 것.**
> y 가 조금만 아래로 내려가도 배경이다.

**안 옮긴 것**: 소탕(SKIP 티켓). PC 는 `-UseSkip` 스위치로만 켜지는데 폰에는 그 스위치를 둘
자리가 없어 뺐다. 넣으려면 **SKIP 티켓을 쓴다는 걸 화면에 밝혀야 한다.**

### 아레나 (`Arena.kt`)
**나보다 약한 상대만** 골라 싸운다. 상대를 8명 넘겨도 못 찾으면 지금 상대와 그냥 도전한다 —
**크리스탈 새로고침은 절대 안 한다**(300 크리스탈). `maxFights = 0` 이면 진입만 시험한다.

- **승패는 화면으로 못 읽는다** → 아레나 점수의 변화로 가른다(오르면 승·내리면 패, 무승부 없음).
  ±1~2 는 OCR 노이즈라 무시하고, 300 을 넘는 점프는 팝업에 가려 0 으로 읽힌 오독이라 버린다.
- 로비 판정은 **'내 전투력이 읽히면 로비'**. 승급·강등 화면은 위치가 제각각이라 하단만 눌러서는
  못 넘긴다 → 하단 '탭하세요' 와 화면 한가운데를 같이 누른다. 그래도 안 되면 뒤로가기 후 재진입.
- 새로고침 팝업을 만나면 **왼쪽 [취소]**. 오른쪽 주황은 크리스탈 300 이라 어느 창에서든 안 누른다.

#### ⚠️ PC 봇을 그대로 옮기면 안 되는 곳 — 전투력 비교
`arena.ps1` 은 숫자만 뽑아 비교한다. 그러면 `59.64M` 은 `5964`, `1.21G` 는 `121` 이 되어
**단위가 섞이는 순간 더 센 상대를 약하다고 오판한다.** 아레나에서 그건 곧 패배다.
폰에서는 `Ocr.readPower` 가 **단위까지 읽어** 실제 크기로 바꾼다.

    59.64M → 59,640,000    1.21G → 1,210,000,000    59M 70K → 59,070,000    1,234 → 1,234

**다음**
| 할 일 | 검증 비용 |
|---|---|
| (콘텐츠는 다 옮겼다) 실기 검증이 남았다 | — |

### 실기 확인 기록
| 날짜 | 확인한 것 | 결과 |
|---|---|---|
| 09-04 | 토벌전 자동 — 진입·반복 도전·점수 읽기·결과창 정리 (끝까지) | ✅ |
| 09-05 | 보상 받기(미션·출석) — `[보상만 받기]` 한 바퀴 | ✅ |
| 09-05 | **시험 모드로 네 콘텐츠 진입** — 퀘스트·토벌전(길드 경로)·일일 던전·아레나 | ✅ |
| 09-05 | 앱 안 자동 업데이트 · 게임 패키지 자동 탐지 | ✅ |

09-05 로 확인된 것:
- **우측 아이콘 열 좌표**(미션 `(1336,1130)` · 달력 `(1336,1300)`) — 제일 의심하던 자리였다.
- **토벌전 길드 경로** — 하단 '길드' 탭 `(905,3000)` → '길드 토벌전' 타일 `(280,1880)`.
- **'던전' 탭을 `(505,2990)` 로 옮긴 판단** — PC 의 `(530,3054)` 는 폰에서 빈 배경이었다.
- 일일 던전·아레나 **진입 화면 판정**(`atDailyEntry` · 아레나는 '내 전투력이 읽히면 로비').

> ⚠️ **진입까지가 확인된 것이지 한 바퀴가 확인된 게 아니다.** 시험 모드는 진입 직후 끝낸다.
> 아래 것들은 아직 한 번도 안 돌아 봤다.

### 아직 실기로 못 본 것
- **퀘스트 순환 한 바퀴** — 띠 수령 · 뽑기 10회 · 가방 상자 · 오븐 · 보스 소환.
  특히 **오븐은 Auto 를 반드시 끄고 나와야 한다**(안 끄면 재화가 샌다). 첫 실행 때 이걸 눈으로 볼 것.
- **일일 던전 한 바퀴** — 전투 반복 · 달성 보상 수령 · `▶` 순환 · 첫 던전 복귀 종료.
- **아레나 실전** — `Ocr.readPower` 가 단위를 제대로 읽는지 로그로 확인할 것.
  `상대 45,200,000 ≤ 나 59,640,000 → 도전` 처럼 **실제 크기**로 찍혀야 한다.
  `5964` 같은 작은 수가 나오면 단위 처리가 깨진 것이다.
- 일일 던전 **소탕**(설정에서 켰을 때만).
